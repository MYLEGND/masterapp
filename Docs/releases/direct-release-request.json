{
  "requestedBy": "Founder",
  "authorization": "Founder explicitly requested recovery of failed direct release #121 together with the integrated business-domain Worker bridge, as one approved release through legend/approved-changes. Cloudflare deployment secrets are configured. The exact analytics regression is fixed, and the Worker release lane is hardened after the prior first-party smoke raced ClientApp warm-up. Retry on the now-clear serialized lane.",
  "purpose": "Retry the same approved unified release with no new product behavior: deploy Protect before activating the Cloudflare route, retain the immutable ClientApp ZIP retry, retry first-party Worker smoke through transient app warm-up, and restore the prior Worker deployment through the Cloudflare deployments API if a smoke/proof fails. Keep Portal/Client/Protect as the already-approved unified targets; exclude Parfait and the static LEGEND Website. Preserve domain ownership, billing, analytics semantics, schema, authorization, and unrelated systems.",
  "requestId": "domain-diagnostics-and-business-routing-unified-release-20260924-retry4",
  "releaseMode": "approved-only",
  "targets": [
    "masterapp-portal",
    "masterapp-client",
    "masterapp-protect"
  ],
  "cloudflareWebsiteRouting": true,
  "websiteRoutingCanaryHost": "camoexterior.com"
}
