{
  "requestedBy": "Founder",
  "authorization": "Founder explicitly authorized the shared Cloudflare business website routing bridge and direct publication through legend/approved-changes after the exact validation run passed. Deploy only the affected Protect/Business Website backend and the shared Cloudflare website-routing Worker.",
  "purpose": "Deploy the validated single-source custom-domain transport. WebsiteDomainBinding remains the only business/domain ownership authority. The Cloudflare Worker only bridges customer vanity hostnames to the existing masterapp-protect Azure origin, preserves the original hostname through an authenticated internal header, leaves all LEGEND-owned mylegnd.com hosts on their existing routes, and fails closed without the shared authority. Reuse or create the one shared bridge secret, deploy the Worker, deploy masterapp-protect, and require camoexterior.com /.well-known/legend-website to return the exact LEGEND binding proof. No per-customer Azure binding, duplicate routing store, schema migration, unrelated app deployment, override, or stacked system.",
  "requestId": "business-domain-worker-bridge-20260924-release",
  "releaseMode": "approved-only",
  "targets": [
    "masterapp-protect"
  ],
  "cloudflareWebsiteRouting": true,
  "websiteRoutingCanaryHost": "camoexterior.com"
}
