{
  "requestedBy": "Founder",
  "authorization": "Founder explicitly requested recovery of failed direct release #121 together with the already-integrated business-domain Worker bridge, as one approved release through legend/approved-changes. The repair/business-domain-worker-bridge-20260924 branch is fully contained in legend/approved-changes and must not be released separately.",
  "purpose": "One release for the current approved source of truth: redeploy AgentPortal, ClientApp, and Protect so the domain-diagnostics batch from #121 reaches every intended app; deploy the shared Cloudflare business website router from the already-integrated repair branch; use bounded immutable-ZIP retry for ClientApp so a transient Kudu/OneDeploy extraction failure cannot leave ClientApp behind; and permit this reviewed routing release to include only the required Portal/Client/Protect companions while still requiring Protect. Build and test once from the exact approved SHA, reuse the exact packaged bytes on every ClientApp retry, preserve existing migrations/authorization/domain ownership, and require final provenance plus custom-domain bridge verification before success. No unrelated Parfait or LEGEND static website deployment.",
  "requestId": "domain-diagnostics-and-business-routing-unified-release-20260924-retry1",
  "releaseMode": "approved-only",
  "targets": [
    "masterapp-portal",
    "masterapp-client",
    "masterapp-protect"
  ],
  "cloudflareWebsiteRouting": true,
  "websiteRoutingCanaryHost": "camoexterior.com"
}
