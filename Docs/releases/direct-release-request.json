{
  "requestedBy": "Founder",
  "authorization": "Founder approved surgical direct ClientApp release to stop AADSTS50020 caused by entering Microsoft OIDC without the canonical client identity preflight.",
  "purpose": "Direct release only ClientApp. Require the existing member sign-in preflight before any non-support Microsoft OIDC challenge when no protected continuation exists, so the canonical ClientIdentityAccessService provisions/synchronizes the external Entra guest and uses redemption when required. The previously failing regression assertion has been corrected to match the intended redirect-to-preflight behavior. The experimental package-gate change was removed; the previously proven package authority is restored. Do not modify subscription activation, Square payments, billing orchestration, subscription management, database schema, AgentPortal, Protect, Parfait, or LEGEND Website.",
  "requestId": "clientapp-aadsts50020-preflight-repair-20260922-3",
  "releaseMode": "approved-only",
  "targets": [
    "masterapp-client"
  ]
}
