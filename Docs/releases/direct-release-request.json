{
  "requestedBy": "Founder",
  "authorization": "Founder explicitly authorized the exact business-scoped Clients/Leads CRM repair and direct ClientApp deployment through legend/approved-changes. Preserve all unrelated working flows.",
  "purpose": "Fix the reproduced ClientApp business CRM Razor failures at their canonical source. Remove the orphaned ClientApp/Views/Clients/Index.cshtml duplicate that shadows the linked canonical AgentPortal Clients view and expects the wrong List<ClientProfile> model. Change all top-level Clients and Leads nested partial references to explicit canonical absolute view paths so BusinessWorkspaceController route context cannot redirect relative partial lookup into /Views/BusinessWorkspace. Retain the real Razor render regression tests for both pages. No copied replacement view, no override, no stacked handler, no database migration, and no deployment of AgentPortal, Protect, LEGEND Website, Parfait, or Cloudflare.",
  "requestId": "business-crm-canonical-razor-resolution-direct-20260924",
  "releaseMode": "approved-only",
  "targets": [
    "masterapp-client"
  ],
  "cloudflareWebsiteRouting": false
}
