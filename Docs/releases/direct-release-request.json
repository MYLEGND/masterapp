{
  "requestedBy": "Founder",
  "authorization": "Founder explicitly requested the narrow post-activation client identity recovery repair be directly released. The shared deployment lane was verified clear immediately before this trigger. Deploy ClientApp only; runtime code remains unchanged from the reviewed surgical fix.",
  "purpose": "Deploy only ClientApp with the bounded recovery worker that retries the existing IClientEntraLifecycleService for active client subscriptions whose ExternalIdentityObjectId is still missing after activation. No billing-orchestrator changes, no schema migration, no duplicate identity system, and no unrelated app deployment.",
  "requestId": "client-post-activation-entra-recovery-20260923-r5",
  "releaseMode": "approved-only",
  "targets": [
    "masterapp-client"
  ]
}
