{
  "requestedBy": "Founder",
  "authorization": "Founder explicitly authorized the surgical custom-domain diagnostic addition and direct publication through legend/approved-changes. Deploy only the applications that serve or render the Connected Domains verification flow.",
  "purpose": "Deploy the existing WebsiteDomainService diagnostic enhancement that reports the exact Cloudflare hostname gate, HTTPS/DCV gate, provider validation errors, LEGEND routing-proof result, and the concrete required action instead of the generic pending message. Preserve all domain ownership, Cloudflare hostname/certificate behavior, DNS instructions, website routing, billing, analytics, content, leads, and unrelated application behavior. No overrides, duplicate verifier, schema migration, or unrelated deployment.",
  "requestId": "business-domain-exact-diagnostics-20260924",
  "releaseMode": "approved-only",
  "targets": [
    "masterapp-portal",
    "masterapp-client",
    "masterapp-protect"
  ]
}
