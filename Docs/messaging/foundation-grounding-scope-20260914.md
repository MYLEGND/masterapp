# Controlled foundation grounding and user scope

Base candidate: 99a5fddf33aae934f8846b897c08a02f4ca7e8e5. Published production merge: 5f236cbc0d69ff2198b7e1c4a45056ffa6a2d83f. Existing integration checkout clean before repair; preserve CEO checkout and unrelated worktrees. No production mutation or release in this repair until verification.

## Confirmed defects and owners

- `FounderLegendConnectLiveMetricsSnapshot.Create` discards the acquired runtime readiness snapshot and emits rounded display values without numeric values or metric semantics. The same projection feeds Founder UI and `legend_system_overview`; model extrapolated global readiness/confidence from translation counters. Lead owns Models/LegendConnectViewModels.cs, Services/LegendFounderAiConversationService.cs, Services/LegendFounderToolAuthority.cs, and FounderLegendConnectLiveMetricsTests.cs.
- Tool catalog is not full application coverage. `legend_client_lead_portfolio` returns Founder aggregate counts; no scheduling, reminders, lead update or website analytics tool is registered. Specialist maps existing owners; do not advertise unavailable actions or add parallel domain implementations.
- Founder-only execution is enforced at conversation and tool boundaries. Conversation history/memory is actor scoped. Retain these restrictions; this is not authorization to expose this global Founder registry to ordinary accounts.
- Privacy review of operational metric drilldowns: specialist translation_platform owns Infrastructure/Messaging/LegendConnectOperations.cs and focused tests after confirming exact boundary. No overlapping edits.

## Shared contracts and acceptance

Preserve metric dictionary/displayValue/tone and tool names. Add authoritative readiness, snapshot timestamp, numeric values and definitions to existing snapshot (additive JSON). No new metric calculator, store, routing authority or provider. Definitions explain observed counts, distinct reuse routes, acquisition vs learning, and limits of inference. Client consumers continue displaying existing values. Model uses general pretrained knowledge while treating organizational claims as evidence-dependent; user-specific memory cannot authorize access or shared training.

Verify projection preservation/rounding/zero requests/readiness provenance; forbidden cross-user diagnostics; catalog semantics and model instruction contract. Run focused existing tests then real controlled inference against repaired application before claiming behavioral improvement. No benchmark-specific answers or changed quality assertions. Existing accepted model failures are not fixed by schema changes alone. Scheduling/marketing requires existing service integration and scoped/idempotent action receipts, not broad SQL or credentials.

## Integrated urgent patch

- Live authenticated browser reproduced HTTP 503 `MESSAGING_TRANSLATION_NOT_READY` on conversation detail; all composer controls disabled. No live test messages sent. `MessagingService.ApplyTranslationPresentationAsync` omitted failed translations, so the complete-page guard rejected the conversation. Translation specialist corrected this owner to retain every authorized original body/quote with the existing localized notice. Authentication, closed-thread checks, quota notice, notification retry and cache admission remain unchanged.
- Readiness specialist corrected missing-authority observations and registry-read failures: unavailable values are explicitly unknown; admission stays fail-closed. This gate describes autonomous acquisition, not every background process or model intelligence. Lead independently reviewed; foundation specialist reviewed task/readiness boundaries with no must-fix findings.
- Lead connected confirmed personal task creation/listing to existing ExecutionEngine. Actor comes from canonical authenticated profile, writes use request operation ID, strict arguments and explicit confirmation. SQLite tests cover fresh-context replay, foreign/delegated collisions and public conversation acknowledgement loss after a real task/log commit. Tasks are not calendar events or delivered reminder notifications.
- Operational drilldowns aggregate usage without user IDs/request references and apply the existing persisted global entitlement rule. Request capability discovery reflects actually available tools.
- Combined Release build: 0 warnings/errors, 46.19s. Focused combined run: **307 passed, 0 failed, 0 skipped**, 36s. Evidence: `/private/tmp/legend-pretrained-results/urgent-messaging-ai-focused.trx`. Includes messaging, application localization, task persistence/isolation, authorization, metrics and routing. This is focused verification, not full regression or production proof.
- Real-model development earlier: 2 formal passes/1 failure; readiness prose still overreached before the subsequent availability repair, and causal diagnosis failed. Those are not clean quality acceptance. No training/checkpoint improvement claimed. Current model remains the pinned Qwen 4B MLX checkpoint; no paid teacher calls added.
- Catalog investigation: existing transient continuation and native warm cache are already wired generically. Manifest has 5,845 entries, including six approved-only entries without built-in presets; these require approved retained translations. Quota exhaustion waits for reset. Neither is fixed by unbounded retry or falsely marking a catalog complete. The account's actual incomplete-catalog disposition remains unverified; no speculative loop/cache replacement added.
- Latest user explicitly authorizes an urgent direct backend build/deployment including these current AI repairs and defers remaining AI deficiencies. Prepare both existing Windows App Service packages; preserve prior successful artifacts from run 34798299334. Do not claim this direct deployment merges the protected production branch or that its old frozen model-quality waiver applies. No new infrastructure, schema changes, permissions or paid capacity authorized/introduced.
