# Chat repair sequence — 2026-09-11

Status: audited and prepared; chat implementation follows the social-creation repair. The user has authorized repair work but explicitly holds messaging and translation deployment until their green light. No current document authorizes deployment or claims production parity.

## Ordered implementation and exit evidence

1. **Social prerequisite.** Apply the independently reviewed single-scope IIS configuration; run controller, storage and processing regressions and inspect published configuration. Current production evidence shows media POST selecting StaticFile before application authorization. Local tests cannot establish Windows IIS repair. Repeat direct/stage routing and controlled post/story/Hac creation after an authorized deployment before calling live creation fixed.
2. **Opening speed and request ownership.** Instrument the existing shared messaging projection with safe stage timing. Correct duplicate refreshes, stale navigation results and read-ack amplification in existing client stores. Preserve actor/language isolation and uncertain-send IDs. Bound web history using the same page authority as mobile. Batch cache reads within the existing service; retain quota, consent and cancellation. Measure warm/cold completed requests and first usable paint separately. See [latency audit](chat-opening-audit-20260911.md).
3. **Receipt hierarchy.** Show one small green Read pill on the latest acknowledged own message covered by authorized shared reader watermarks. Show small red Sent pills for newer acknowledged messages; retain distinct pending/failed states. Preserve privacy and current group read semantics. Use existing design/localization authorities. See [interaction audit](chat-interaction-audit-20260911.md).
4. **Reactions.** Extend the existing messaging service, persistence and DTOs with idempotent set/replace/remove and typed actor uniqueness. Then connect double-tap like, long-press default palette and plus picker across clients using existing realtime reconciliation. Test retries, concurrent updates, authorization and accessibility before UI claims.
5. **Real shared content and attachments.** Carry typed source references through existing message persistence and recipient projection; use current social visibility/media authority. Render viewable cards and explicit expired/restricted states. Add native download adapters to the existing clean-attachment authority. External shares must carry a resolvable canonical link or permitted file, never bearer tokens or only descriptive text. See [sharing audit](content-sharing-audit-20260911.md).
6. **Combined verification.** Run focused negative and regression tests, then combined suites and real web/iOS/Android checks. Preserve the baseline's nine known native failures as separately disclosed; they do not excuse new messaging regressions. Record exact source/artifacts, executed versus blocked checks, latency distributions and actual content opening. Keep all messaging publication/deployment held until explicit user release approval.

## Current evidence and limits

The supplied log measures thread HTTP waits of 26.026, 17.780 and 4.671 seconds, versus 6–18 milliseconds decoding. It does not identify server SQL/provider stage duration or deployed revision. This establishes an unacceptable wait but does not prove one specific database bottleneck. Proposed performance targets in the detailed audit are acceptance goals, not achieved measurements or claims of parity with another product.

Both native share producers demonstrably flatten content into text before the messaging contract. Existing mobile attachment rows are not interactive. These are code-confirmed gaps, independently auditable without private account data. New functionality must be validated against the same shared backend across all clients; visual changes alone cannot close these defects.
