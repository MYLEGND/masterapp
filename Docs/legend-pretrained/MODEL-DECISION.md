# LEGEND foundation target — Qwen3.8-27B qualification pending

CURRENT BILLING AUTHORITY: all unused experiment authorization is withdrawn. No new paid resources, quota/support requests or recurring commitments. Prior approved envelopes below are historical only.

Latest user direction: qualify Qwen/Qwen3.8-27B directly for production. Comparative selection is deferred. This is the intended target, not a tested capability or deployment claim. Existing $20/four-hour approval covers bounded qualification only; recurring hosting requires separate approval.


**No production foundation has been selected.** The active requirement is a fair remote comparison of official `Qwen/Qwen3-4B-Instruct-2507` and `Qwen/Qwen3.8-27B`, through the corrected authoritative LEGEND pipeline. Historical Mac experiments and earlier hosted-model canaries do not select the winner.

## Current architecture and configuration

The existing `LegendFounderAiConversationService.ReplyAsync` resolves identity, conversation, language and immutable provider policy, then uses the existing `ILegendConnectModelInferenceTransport`. Approved organizational evidence, governed research, memory and tool execution remain existing authorities. Ordinary conversation no longer requires curriculum semantic-family coverage or a matching compiled transition. Evidence and permission gates remain for organizational assertions, promotion and actions.

`LegendConnect:Foundation` is the central serving configuration. It defaults disabled with empty model/revision/endpoint/credential/Azure resource ID/engine version/tool parser until a deployment is actually qualified. Engine is Vllm; actual generation/parser settings and hosting receipts must match. The registry alone chooses promoted checkpoints; no manually selected adapter bypasses promotion. Unavailable controlled inference is a service limitation, never a silent hosted primary fallback.

`IndependentAnswering` blocks external generative inference, planning, grading and escalation while permitting separately authorized research and Azure translation. `NativeOnly` also blocks those external dependencies. The existing configured research discovery transport uses OpenAI Responses web search: it is therefore blocked under either restricted answering policy before credential/client access. Its absence is reported; it is not relabeled independent research. Existing public page retrieval and Azure translation are distinct nongenerative dependencies. A recorded research fixture is repeatable application evidence, not live research proof.

OpenAI remains an explicitly permitted external escalation/teacher. Its output has external provenance and is restricted from training unless applicable rights are established; no private agreement granting competitive training rights has been verified. Controlled training defaults disabled and external teacher learning defaults false. No model API is required to judge deterministic foundation controls.

## Candidate evidence and remote plan

Exact verified official metadata is in `REMOTE-MODEL-REQUIREMENTS.json`; pricing/quota in `REMOTE-CAPACITY-RECEIPT.json`; fixed thresholds and the proposed bounded budget in `REMOTE-COMPARISON-PLAN.md`. Required 27B revision is `1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0`, Apache-2.0. Official BF16 repository is approximately55.6GB; FP8/other conversions have distinct provenance. Actual remote memory, latency, tool capability, Haitian Creole quality and training compatibility are unverified.

There is no existing GPU capacity. Proposed East US2 A10080GB requires quota increases from zero family vCPUs and10 regional vCPUs to24. The user approved a maximum $20 total incremental cost and four cumulative compute hours, whichever comes first, including all ancillary charges. No paid compute is provisioned. Acquisition remains blocked until cost and independent shutdown controls satisfy that authorization. Confirmed subscription policy is PayAsYouGo_2014-09-01 with spendingLimit Off; Azure does not offer a configurable hard spending limit for this subscription type. Timed deallocation is not a guaranteed invoice cap. No model assets or datasets may be acquired on the laptop.

## Historical evidence — superseded for selection

`LOCAL-MODEL-COMPARISON.json` records narrow Mac4B/8B model-level checks. `LOCAL-REPLY-DEVELOPMENT.json` and `LOCAL-8B-DEVELOPMENT.json` record actual application failures, including grounding, reasoning and unnecessary tools; they prevent treating those narrow successes as acceptance. Framing/tool-exposure repairs need both requested candidates retested remotely.

`LOCAL-TRAINING-EXPERIMENT.json` records an actual QLoRA checkpoint and controlled before/after run: held-out4/8 versus baseline0/8, with unacceptable regressions. The adapter was rejected, never promoted. This is not autonomous-learning completion. Eligible synthetic data/provenance and diagnostic receipts are in private remote storage, verified before local deletion. `LOCAL-CLEANUP-RESULT.json` records about6.93GB recovered; no experiment weights/checkpoints/datasets remain locally.

Earlier hosted canaries in `MODEL-CANARIES.json`, `MODEL-MEDIUM-CANARIES.json` and `FOUNDATION-REPLY-CANARY.json` are explicitly external historical observations only. They do not establish independent operation and their earlier primary-engine selection is superseded.

## Completion and release limits

Selection requires StageA compatibility, StageB quality gates and StageC finalist acceptance through one frozen app/configuration. Actual learning additionally requires rights-approved material, a remote training run, a valid checkpoint consumed by runtime, new held-out capability and acceptable regressions under blocked external generation. None of those remote model results has yet been performed. Candidate read-only production-data and live Founder checks remain blocked by protected access. Production routing is unchanged; no release is authorized by this document.