# Remote comparison: approval and evidence contract

Status: user approved the initial experiment, total incremental cost at most $20 and cumulative compute at most four hours, whichever comes first. All setup/downloads/disks/storage/network charges count. Only necessary quota increases are authorized; no extension, recurring compute or production deployment. Implementation continues; no model selected, paid compute provisioned or remote model evaluation performed. Historical Mac results are excluded from selection. This plan permits no laptop weights, model execution, training data, or checkpoints.

## Capacity and proposed initial spending envelope

CONFIRMED on 2026-09-12: LEGEND subscription `a543b54e-8a42-4a4b-bd4d-bc13573b1c48` has no VM/GPU capacity. Its Compute provider is now Registered. East US 2 lists `Standard_NC24ads_A100_v4` without an SKU restriction, but NCADS_A100_v4 family quota is **0** and regional total quota **10** vCPUs. The proposed VM requires **24** of each. SKU eligibility does not guarantee physical capacity. A quota increase is required before allocation. No production web host will run models. The isolated candidate application and model worker run together remotely; use Azure control-plane Run Command with no public inbound model port or public IP. The model endpoint is authenticated even on loopback.

Approved initial resource envelope, pending quota and verified cost controls: one isolated Linux `Standard_NC24ads_A100_v4` named `legend-foundation-compare-20260912`, in resource group `LEGEND-Foundation-Experiments`, East US 2 (1 A100 80 GB, 24 vCPU, 220 GiB system memory), one 64 GiB Standard SSD OS disk, model assets on the VM's included temporary disk. A four-hour initial Stage A/B window costs at most **$14.692 compute** at the verified public on-demand **$3.673/hour** rate. Authorized total initial ceiling: **$20 USD**, including setup, failed attempts, shutdown margin, disk operations and bounded storage/transfer. Do not use Spot for this controlled latency comparison; interruptions would confound measurements. Do not automatically extend the deadline or provision another VM.

ESTIMATE, not measured: acquisition/setup 45–90 minutes; compatibility 15–30 minutes; focused comparison 45–90 minutes. Stop when the remaining time cannot fit the next bounded case. An incomplete round remains incomplete. Training/finalist acceptance requires unused approved capacity; any extension requires new explicit authorization after a candidate qualifies; the initial budget does not promise completion of every stage.

Official Azure retail API queried on 2026-09-12: East US 2 Linux A100 rate above; Standard SSD E6 LRS $4.80/month plus $0.002/10,000 operations; general block blob Hot LRS first tier $0.0184/GB-month. Disk four-hour estimate uses $4.80/730×4 ≈ $0.027, not a guaranteed invoice. The [official bandwidth price](https://azure.microsoft.com/en-us/pricing/details/bandwidth/) for transfers between North American regions is $0.02/GB; a capped 5 GB results transfer to the existing East US archive is approximately $0.10. Reverify the account quote before provisioning; taxes and agreement-specific rates are excluded. Public model ingress is not an answering API charge. Full weights should be reacquired from pinned official sources remotely rather than archived indefinitely. Retain at most 5 GB of authorized datasets/checkpoints/results for at most 30 days within the initial budget: about $0.092 at the cited monthly storage rate, excluding transactions/transfer. Retention charges count within $20; no indefinite storage commitment is authorized by this experiment. Preservation/expiry must be explicit before acquisition. Current historical archive is only 218,847 bytes; its exact invoice cost has not been measured.

Source: [Azure Retail Prices API](https://learn.microsoft.com/rest/api/cost-management/retail-prices/azure-retail-prices), [A100 VM specifications](https://learn.microsoft.com/azure/virtual-machines/sizes/gpu-accelerated/ncadsa100v4-series). Exact API response metadata is retained in the remote capacity receipt. No rate is a reserved-capacity offer or assurance of allocation.

## Required hard cost controls before acquisition

CONFIRMED subscription policy: `PayAsYouGo_2014-09-01`, `spendingLimit: Off`. Azure does not offer a configurable PAYG spending limit ([official documentation](https://learn.microsoft.com/en-us/azure/cost-management-billing/manage/spending-limit)). A scheduled deallocation bounds intended execution but cannot guarantee a dollar ceiling during a control-plane failure. No paid resource may be provisioned on the claim that an alert or schedule guarantees the approved cap. The limit remains a provisioning blocker. New-VNet outbound acquisition and any explicit egress resource charges also remain unverified; the no-public-IP topology above is a proposal, not an established working deployment.

Provisioning must fail closed unless an independent Azure control-plane shutdown/deallocation schedule is installed and read back for the exact VM and experiment deadline. A guest timer is a second control, not the sole spend control. No restart policy, VM scale set, autoscaler or retry may recreate compute. Use one VM, concurrency one, no inference retry during scoring, at most one setup retry within the same absolute deadline. Install failure cleanup before downloading assets. Bound generated tokens, context, disk consumption and training time through the existing runtime/training configuration. Deallocate on success, error, cancellation or deadline, then verify instance billing state. Delete only experiment OS disk/network resources after necessary evidence is remotely preserved; retain approved blobs with explicit expiry. An alert alone does not satisfy this gate. These cloud controls are not yet provisioned or verified; spend approval is conditional on proving them before model acquisition.

## Fixed comparison gates, established before scoring

Both candidates use the existing `LegendFounderAiConversationService.ReplyAsync`, model transport, knowledge snapshot, authenticated permissions, tool fixtures, research authority, translation cache and deadlines. Freeze Git SHA, configuration hash, model/tokenizer/template hashes, approved knowledge/tool fixture hashes and evaluation scenario manifest hash before each round. A shared fix invalidates affected results for both models. No paid model grades the comparison. Record exact prompts/responses with sensitive information protected; datasets remain remote.

Stage A: every compatibility case must pass: pinned identity/host, authenticated endpoint, correct final-text/reasoning separation, structured tool arguments, cancellation/deadline, checkpoint identity/loading, external-answering isolation, permissions and truthful provenance. Zero forbidden external generative calls. Stop a candidate after an integration failure; repair the shared owner before further expensive scoring.

Stage B: all previously failing ordinary tasks plus 40 fresh paired scenarios, five per bucket: conversation; intent/constraints/corrections; reasoning/multiturn; articulation; approved/conflicting/missing evidence; Haitian Creole/language; appropriate tool use; deliberate non-use/limitations. Development examples are labeled separately. Score correctness, instruction adherence, grounding and language/articulation each 0/1/2 under a fixed rubric. Require mean ≥7/8, each bucket ≥6.5/8, at least 38/40 correctly completed tasks, at least 4/5 correct in every bucket and every known critical regression resolved. Zero permission violations, fabricated completed actions, fabricated evidence or cross-user leaks: these are hard failures regardless of averages. Blind model labels for qualitative review where practical; Haitian Creole quality needing fluent human review remains pending, not automatically passed.

Start with original official weights and documented model-specific templates/parsers. Compare at 32,768 context, 120-second application deadline, concurrency one, 1,024 final-output budget, no scoring retries. Supported reasoning requires an explicitly bounded separate allowance where the engine counts reasoning in its output budget; record actual consumption and rerun affected configurations fairly. At most two practical serving configurations per candidate initially; no untracked tuning. A 27B reasoning mode must not be copied into the nonthinking 4B configuration. Quantized artifacts require their own pinned provenance and qualification. Application permission decisions never move into model prompts.

Stage C: expand only qualified candidates to tool-family coverage, concurrency one/two within measured memory, timeout/cancellation/duplicate/failure injection, full platform contracts, and a diagnosed-gap learning experiment. Tool families include diagnostics, approved knowledge, research, memory, curriculum/teaching, escalation, learning activation, repository/repair/validation/release diagnostics. Consequential actions use authorized nonproduction fixtures. Use a small separate live research/Azure-cache check, never repeated paid identical searches or translations. IndependentAnswering permits authorized research/Azure but blocks all external generation; NativeOnly additionally blocks those dependencies. Optional OpenAI escalation is a separate result, never independent success.

Learning acceptance requires base failure, rights/privacy-approved training examples, real bounded remote adapter training, valid checkpoint, runtime consumption and improvement on fresh scenario-separated held-out tasks with prompts/retrieval/settings fixed and prior capabilities preserved. A rejected adapter or completed registry record is not success. Use the existing promotion/rollback authorities without policy changes. Never reuse a 4B adapter with 27B.

Report sample count, all failures, first-output/completion times, retry counts, memory, throughput, unnecessary tools, external calls, training metrics, idle time and total cost per correctly completed task. Small-sample percentiles are descriptive only. At a stated illustrative 10,000 requests/month, a continuously allocated proposed A100 costs $2,681.29/month compute alone, or $0.268/request before failures/storage/tools; this is arithmetic, not a production recommendation. Production sizing and utilization require remote measurements. Neither model qualifies until the quality gates pass; a lower escalation rate alone is not improvement.

Required candidate-on-production-data validation and live authenticated Founder checks remain blocked by unavailable protected runner credentials/session. Currently deployed app tests do not prove unpublished candidate behavior. Production routing is unchanged.
