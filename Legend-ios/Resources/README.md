# Native resource ownership

Application resources belong under `Legend/Resources` so the Legend target can
own them. This directory is reserved for repository-level native release
artifacts only; it must not contain credentials, provisioning profiles, or
production configuration.
