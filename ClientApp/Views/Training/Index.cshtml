@{
    ViewData["Title"] = "Trainings";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="training-page container py-4 training-container">
    <div class="training-hero mb-4">
        <div class="training-hero-kicker">Legend™ Education</div>
        <h1 class="training-hero-title m-0">@ViewData["Title"]</h1>
        <p class="training-hero-sub m-0 mt-2">Access training modules, certifications, and learning materials to level up your team.</p>
    </div>

    <section class="training-controls-card mb-4">
        <div class="training-controls-head">
            <div>
                <div class="training-info-kicker">Training Library</div>
                <h2 class="training-info-title mb-1">Browse and preview trainings</h2>
                <p class="training-info-copy mb-0">Select or search a training and preview it instantly below.</p>
            </div>
        </div>

        <div class="row g-3 align-items-start">
            <div class="col-12 col-lg-6">
                <label for="training-dropdown" class="training-control-label">Browse Trainings</label>
                <select id="training-dropdown" class="form-select training-premium-field">
                    <option value="">Select a training…</option>
                    @if (ViewBag.Trainings != null)
                    {
                        foreach (var tr in ViewBag.Trainings)
                        {
                            <option value="@tr.File">@tr.Name</option>
                        }
                    }
                </select>
                <div class="training-control-help">Choose a training to preview below.</div>
            </div>

            <div class="col-12 col-lg-6">
                <label for="training-search" class="training-control-label">Search Trainings</label>
                <div class="training-search-wrap">
                    <span class="training-search-icon">⌕</span>
                    <input type="text" id="training-search" class="form-control training-premium-field training-premium-input" placeholder="Search training...">
                </div>
                <ul id="training-search-results" class="training-search-results" style="display:none;"></ul>
                <div class="training-control-help">Type to filter trainings instantly.</div>
            </div>
        </div>
    </section>

    <div id="training-empty" class="training-empty-state mb-4">
        <div class="training-empty-chip">Preview</div>
        <div class="training-empty-title">Select a training to preview it here</div>
        <div class="training-empty-text">Use browse or search above. Supported files open directly in this view.</div>
    </div>

    <div id="training-embed" class="training-preview-shell mb-4" style="display:none;">
        <div class="training-preview-head">
            <h3 id="training-title" class="training-preview-title m-0"></h3>
        </div>
        <div class="training-preview-frame">
            <iframe id="training-frame"
                    src=""
                    width="100%"
                    height="850"
                    class="training-frame"
                    oncontextmenu="return false;">
            </iframe>
        </div>
    </div>
</div>

<style>
    :root{
        --gold:#c99b36;
        --gold2:#b68525;
        --gold3:#f0cf78;
        --line:rgba(201,155,54,.44);
        --ink:#e9f0ff;
        --muted:rgba(226,236,255,.82);
        --bg:#091427;
        --shadow:0 24px 58px rgba(3,8,18,.45);
        --r18:18px;
    }

    .training-page{
        background:
            radial-gradient(1200px 420px at 0% 0%, rgba(201,155,54,.12), transparent 58%),
            radial-gradient(900px 320px at 100% 0%, rgba(102,124,168,.12), transparent 56%),
            var(--bg);
        color:var(--ink);
        border-radius:var(--r18);
        padding:12px;
    }

    .training-hero,
    .training-controls-card,
    .training-preview-shell,
    .training-empty-state{
        border:1px solid var(--line);
        border-radius:var(--r18);
        background:linear-gradient(180deg,#132747,#0d1f3c);
        box-shadow:var(--shadow);
        position:relative;
        overflow:hidden;
    }

    .training-hero::before,
    .training-controls-card::before,
    .training-preview-shell::before,
    .training-empty-state::before{
        content:"";
        position:absolute;
        left:0; right:0; top:0;
        height:4px;
        background:linear-gradient(90deg,var(--gold2),var(--gold3),var(--gold2));
    }

    .training-hero{ padding:22px 20px 20px; }
    .training-controls-card{ padding:20px 18px 18px; }
    .training-controls-head{ margin-bottom:16px; }

    .training-hero-kicker,
    .training-info-kicker,
    .training-empty-chip{
        display:inline-block;
        font-weight:900;
        letter-spacing:.12em;
        text-transform:uppercase;
        font-size:.74rem;
        color:#f3d995;
    }

    .training-hero-title,
    .training-info-title,
    .training-preview-title,
    .training-control-label,
    .training-empty-title{ color:var(--gold3); font-weight:900; }

    .training-hero-sub,
    .training-info-copy,
    .training-control-help,
    .training-empty-text{ color:var(--muted); line-height:1.7; }

    .training-control-label{ display:block; margin-bottom:8px; }
    .training-control-help{ margin-top:8px; font-size:.92rem; }

    .training-premium-field{
        border:1px solid rgba(201,155,54,.58);
        border-radius:14px;
        padding:.75rem .9rem;
        background:#f8fbff;
        color:#0f1d38;
    }

    .training-premium-field:focus{
        border-color:rgba(240,207,120,.92);
        box-shadow:0 0 0 .22rem rgba(201,155,54,.22);
    }

    .training-search-wrap{ position:relative; }
    .training-search-icon{ position:absolute; left:12px; top:50%; transform:translateY(-50%); opacity:.55; font-weight:900; }
    .training-premium-input{ padding-left:36px; }

    .training-search-results{
        margin:10px 0 0;
        padding:6px;
        list-style:none;
        border-radius:16px;
        border:1px solid var(--line);
        background:rgba(9,21,39,.96);
        max-height:280px;
        overflow-y:auto;
        box-shadow:0 14px 34px rgba(3,8,18,.34);
    }

    .training-search-results li{
        padding:10px;
        border-radius:12px;
        cursor:pointer;
        color:#edf3ff;
    }

    .training-search-results li:hover{ background:rgba(201,155,54,.16); }

    .training-preview-head{
        padding:16px 18px;
        border-bottom:1px solid var(--line);
        background:linear-gradient(180deg, rgba(201,155,54,.18), rgba(18,40,73,.16) 62%, transparent 100%);
    }

    .training-preview-frame{ padding:18px; }

    .training-frame{
        border-radius:14px;
        border:1px solid var(--line);
        background:#fff;
    }

    .training-empty-state{ padding:22px 18px; }

    .training-empty-chip{
        display:inline-flex;
        align-items:center;
        justify-content:center;
        padding:6px 12px;
        border-radius:999px;
        border:1px solid rgba(201,155,54,.55);
        background:linear-gradient(180deg, rgba(201,155,54,.22), rgba(201,155,54,.10));
        font-weight:900;
    }

    @@media (max-width: 767.98px) {
        .training-hero-title { font-size: 1.5rem; }
    }
</style>

<script>
document.addEventListener("DOMContentLoaded", function() {
    // Dropdown
    const dropdown = document.getElementById('training-dropdown');
    const embedContainer = document.getElementById('training-embed');
    const iframe = document.getElementById('training-frame');
    const title = document.getElementById('training-title');
    const emptyState = document.getElementById('training-empty');

    dropdown.addEventListener('change', function() {
        const file = this.value;
        if (!file) {
            embedContainer.style.display = 'none';
            if (emptyState) emptyState.style.display = 'block';
            iframe.src = '';
            title.textContent = '';
            return;
        }
        embedContainer.style.display = 'block';
        if (emptyState) emptyState.style.display = 'none';
        title.textContent = this.options[this.selectedIndex].text;
        iframe.src = `/training/${file}`;
    });

    // Search
    const searchInput = document.getElementById('training-search');
    const searchResults = document.getElementById('training-search-results');

    // Gather all trainings
    const allTrainings = [];
    @if(ViewBag.Trainings != null)
    {
        foreach(var tr in ViewBag.Trainings)
        {
            <text>
                allTrainings.push({name:"@tr.Name", file:"@tr.File"});
            </text>
        }
    }

    searchInput.addEventListener('input', function() {
        const query = this.value.toLowerCase();
        searchResults.innerHTML = '';
        if (!query) {
            searchResults.style.display = 'none';
            return;
        }
        const matches = allTrainings.filter(t => t.name.toLowerCase().includes(query));
        if(matches.length === 0){
            searchResults.style.display = 'none';
            return;
        }
        matches.forEach(t => {
            const li = document.createElement('li');
            li.style.cursor = 'pointer';
            li.textContent = t.name;
            li.addEventListener('click', function() {
                embedContainer.style.display = 'block';
                if (emptyState) emptyState.style.display = 'none';
                title.textContent = t.name;
                iframe.src = `/training/${t.file}`;
            });
            searchResults.appendChild(li);
        });
        searchResults.style.display = 'block';
    });
});
</script>
