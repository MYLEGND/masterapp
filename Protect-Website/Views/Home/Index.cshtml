@{
    ViewData["Title"] = "Home";
    ViewData["PageKey"] = "home";
    var bookingLink = "https://outlook.office.com/book/LEGEND@mylegnd.com/?ismsaljsauthenabled=true";
}

<style>
:root {
    --bg: #ffffff;
    --ink: #0f172a;
    --muted: rgba(15, 23, 42, 0.72);
    --muted-2: rgba(15, 23, 42, 0.58);
    --surface: #0f172a;
    --surface-soft: #111c32;
    --ink-light: #e8edf7;
    --gold: #a68023;
    --gold-strong: #c79931;
    --gold-border: rgba(199, 153, 49, 0.72);
    --gold-border-soft: rgba(199, 153, 49, 0.50);
    --shadow: 0 16px 46px rgba(0, 0, 0, 0.12);
    --shadow-soft: 0 12px 28px rgba(0, 0, 0, 0.10);
    --radius: 16px;
    --radius-lg: 20px;
}

body {
    background: var(--bg) !important;
    font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
    color: var(--ink);
}

.page-wrap {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0 20px 88px;
}

.section {
    padding: 48px 0;
}

.section-head {
    text-align: center;
    margin-bottom: 24px;
}

.section-title {
    margin: 0;
    font-size: clamp(1.9rem, 3vw, 2.3rem);
    font-weight: 900;
    letter-spacing: 0.02em;
    color: var(--gold-strong);
    position: relative;
}
.section-title::after{
    content:"";
    display:block;
    width: 88px;
    height: 2px;
    margin: 10px auto 0;
    background: linear-gradient(90deg, rgba(199,153,49,0), var(--gold-strong), rgba(199,153,49,0));
}

.section-sub {
    margin: 10px auto 0;
    max-width: 820px;
    color: var(--ink);
    font-size: 1.05rem;
    line-height: 1.7;
}

.grid-3 {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
}

.grid-2 {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 16px;
}

@@media (max-width: 992px) {
    .grid-3 { grid-template-columns: 1fr; }
    .grid-2 { grid-template-columns: 1fr; }
}

.shell-dark {
    background: var(--surface) !important;
    color: var(--ink-light) !important;
    border: 1.5px solid var(--gold-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-soft);
    position: relative;
    overflow: hidden;
}

.shell-dark::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(800px 320px at 18% 0%, rgba(166,128,35,0.18), transparent 60%),
                radial-gradient(520px 220px at 100% 0%, rgba(166,128,35,0.12), transparent 68%);
}

.shell-dark > * { position: relative; z-index: 1; }

.card, .stat, .faq, .proof-card, .process-card {
    background: var(--surface-soft);
    color: var(--ink-light);
    border: 1.2px solid var(--gold-strong);
    border-radius: var(--radius);
    padding: 16px 16px;
    box-shadow: 0 14px 30px rgba(0,0,0,0.16);
}

.card h3, .proof-card h3, .process-card h4 { margin: 0 0 6px; color: var(--gold-strong); }
.card p, .proof-card p, .process-card p { margin: 0; color: #ffffff; line-height: 1.7; }

.pill-title {
    font-size: .8rem;
    text-transform: uppercase;
    letter-spacing: .14em;
    color: rgba(248, 250, 252, 0.78);
    font-weight: 900;
    margin: 0 0 8px;
}

.btn {
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    border-radius: 999px;
    font-weight: 900;
    transition: transform .16s ease, box-shadow .16s ease, border-color .16s ease, background .16s ease, color .16s ease;
    user-select: none;
    white-space: nowrap;
    padding: 12px 18px;
    border: 1.4px solid transparent;
}

.btn-primary {
    background: linear-gradient(180deg, var(--gold-strong), var(--gold));
    color: #fff;
    border-color: var(--gold-border);
    box-shadow: 0 16px 34px rgba(0,0,0,0.20);
}
.btn-primary:hover { transform: translateY(-2px); color: #fff; }

.btn-secondary {
    background: #fff;
    color: var(--ink);
    border-color: var(--gold-strong);
    border-width: 1.8px;
    box-shadow: 0 12px 26px rgba(0,0,0,0.14);
}
.btn-secondary:hover {
    transform: translateY(-2px);
    color: var(--ink); /* keep text dark on hover */
    background: #fff;  /* prevent white-to-light flip */
}

.badge-bar {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 14px;
    border-radius: 999px;
    border: 1.2px solid rgba(232,237,247,0.25);
    background: rgba(255,255,255,0.08);
    color: var(--ink-light);
    font-weight: 800;
    font-size: .95rem;
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.10);
}

.hero-grid {
    display: grid;
    grid-template-columns: 1.1fr;
    gap: 20px;
}

.hero h1 {
    margin: 0 0 12px;
    font-size: clamp(2.2rem, 3.8vw, 2.9rem);
    line-height: 1.08;
    color: #dfe6f0;
    font-weight: 900;
}

.hero p.lead {
    margin: 0 0 14px;
    color: rgba(248,250,252,0.86);
    font-size: 1.05rem;
    line-height: 1.7;
    max-width: 760px;
}

.hero-benefits {
    margin: 14px 0 0;
    padding: 0;
    list-style: none;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 10px;
}

.hero-benefits li {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    color: rgba(248,250,252,0.9);
    font-weight: 700;
    line-height: 1.4;
}

.hero-benefits .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--gold);
    margin-top: 6px;
}

.cta-row {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 14px;
}

.micro-note {
    margin-top: 8px;
    color: rgba(248,250,252,0.92);
    font-size: .95rem;
    font-weight: 700;
}

@@media (max-width: 576px){
    .cta-row { gap: 12px; }
    .cta-row > a { width: 100%; justify-content: center; }
    .hero h1 { font-size: clamp(1.9rem, 6vw, 2.3rem); line-height: 1.12; }
    .hero p.lead { font-size: 1rem; line-height: 1.6; }
}

.trust-strip {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 12px;
}

.stat {
    text-align: center;
    background: var(--surface);
    border: 1.2px solid var(--gold-strong);
    border-radius: var(--radius);
    padding: 14px 12px;
    box-shadow: 0 12px 28px rgba(0,0,0,0.14);
}

.stat .value { display: block; font-size: 1.4rem; font-weight: 900; color: #fff; }
.stat .label { color: rgba(232,237,247,0.82); font-weight: 700; margin-top: 4px; }

.proof-card {
    display: grid;
    grid-template-columns: 1fr;
    gap: 8px;
}

.proof-quote {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--ink);
}

.rating-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 12px;
    border-radius: 999px;
    background: rgba(199,153,49,0.20);
    color: #f8fafc;
    font-weight: 800;
    font-size: .95rem;
}

.process {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
}
@@media (max-width: 992px) { .process { grid-template-columns: 1fr; } }

.process-card h4 { margin: 0 0 6px; font-size: 1.1rem; font-weight: 900; }
.process-card .badge-num {
    width: 34px;
    height: 34px;
    border-radius: 12px;
    background: rgba(166,128,35,0.16);
    color: var(--ink);
    font-weight: 900;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 8px;
}

.faq {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
}
@@media (max-width: 992px) { .faq { grid-template-columns: 1fr; } }

.faq h4 { margin: 0 0 6px; font-size: 1.05rem; font-weight: 900; color: #f8fafc; }
.faq p { margin: 0; color: #f1f5f9; line-height: 1.6; }

.cta-banner {
    padding: 28px 20px;
    text-align: center;
}

.cta-banner h3 { margin: 0 0 10px; font-size: 1.6rem; font-weight: 900; }
.cta-banner p { margin: 0 0 14px; color: rgba(232,237,247,0.8); }

.trust-note {
    font-size: .95rem;
    color: rgba(255,255,255,0.78);
}

a.btn:focus-visible { outline: 2px solid var(--gold); outline-offset: 2px; }

/* --- Signature Experience section --- */
.experience {
    margin-top: 8px;
    padding: 58px 24px;
    background: var(--surface) !important;
    background-color: var(--surface) !important;
    border: 1.8px solid var(--gold-border);
    border-radius: 22px;
    box-shadow: 0 22px 46px rgba(0,0,0,0.18);
    position: relative;
    overflow: hidden;
}
.experience::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
        radial-gradient(820px 340px at 12% 0%, rgba(199,153,49,0.16), transparent 60%),
        radial-gradient(640px 260px at 88% 0%, rgba(199,153,49,0.12), transparent 70%);
}
.experience::after{
    content:"";
    position:absolute;
    inset:0;
    background: linear-gradient(180deg, rgba(0,0,0,0.18), rgba(0,0,0,0.10));
    pointer-events:none;
}
.experience > * { position: relative; z-index: 1; }
.experience h2 {
    margin: 0 0 14px;
    font-size: clamp(2rem, 3.8vw, 2.6rem);
    font-weight: 900;
    letter-spacing: -0.01em;
    color: #dfe6f0;
}
.experience p.lead {
    margin: 0 0 16px;
    color: rgba(232,237,247,0.88);
    font-size: 1.07rem;
    line-height: 1.75;
    max-width: 900px;
}
.experience-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 14px;
    margin-top: 12px;
}
.experience-card {
    background: var(--surface-soft);
    border: 1.3px solid var(--gold-strong);
    border-radius: 16px;
    padding: 14px 14px;
    color: #f8fafc;
    box-shadow: 0 12px 30px rgba(0,0,0,0.18);
}
.experience-card h4 {
    margin: 0 0 6px;
    font-size: 1.05rem;
    font-weight: 900;
    color: #fff;
}
.experience-card p {
    margin: 0;
    color: rgba(232,237,247,0.82);
    line-height: 1.6;
}
@@media (max-width: 768px) {
    .experience { padding: 46px 18px; }
}

</style>

<div class="page-wrap">

    <div style="text-align:center; margin: 18px 0 6px; font-weight: 900; font-size: clamp(2.15rem, 4vw, 2.75rem); letter-spacing: -0.01em; color: var(--gold-strong);">
        Legend™ Legacy Protection
    </div>

    <!-- Optional slim utility strip -->
    <div class="section" style="padding: 12px 0 8px;">
        <div class="shell-dark" style="padding: 10px 14px; display:flex; gap:10px; align-items:center; justify-content: space-between;">
            <span class="trust-note">Licensed professionals. No-pressure assessment.</span>
            <div class="cta-row" style="margin-top:0; gap:8px;">
                <a asp-controller="RiskAssessment" asp-action="Index" class="btn btn-primary" data-cta="hero_start_assessment" data-lead-trigger="true" data-lead-interest="assessment">Start Assessment</a>
                <a href="@bookingLink" target="_blank" rel="noopener" class="btn btn-secondary" data-cta="hero_book_call">Book a Call</a>
            </div>
        </div>
    </div>

    <!-- HERO -->
    <section class="section shell-dark hero" style="padding: 34px 24px;">
        <div class="hero-grid">
            <div>
                <h1>Protect your income, family, and momentum with a clean plan.</h1>
                <p class="lead">We organize your financial life, close coverage gaps, and build a protection plan you can actually keep—without noise or pressure.</p>

                <ul class="hero-benefits">
                    <li><span class="dot"></span>Clarity on what you have, what’s missing, and what to fix first.</li>
                    <li><span class="dot"></span>Coverage that defends income, family stability, and your long-term goals.</li>
                    <li><span class="dot"></span>Simple next steps with licensed professionals.</li>
                    <li><span class="dot"></span>One plan that stays documented, updated, and easy to maintain.</li>
                </ul>

                <div class="cta-row">
                    <a asp-controller="RiskAssessment" asp-action="Index" class="btn btn-primary" data-cta="hero_start_assessment" data-lead-trigger="true" data-lead-interest="assessment">Start Assessment</a>
                    <a href="@bookingLink" target="_blank" rel="noopener" class="btn btn-secondary" data-cta="hero_book_call">Book a Call</a>
                </div>
                <div class="micro-note">No obligation. We start with understanding your risks and goals.</div>
            </div>
        </div>
    </section>

    <!-- SIGNATURE CLIENT EXPERIENCE (must sit immediately after hero) -->
    <section class="experience section" style="padding-top: 0;">
        <h2>The Legend Client Experience</h2>
        <p class="lead">Built to be calm, documented, and decisive. We don’t just quote policies—we run a protection playbook that feels nothing like the industry norm.</p>
        <div class="experience-grid">
            <div class="experience-card">
                <h4>Documented decisions</h4>
                <p>Clear options, real numbers, and full transparency, so you understand exactly what you’re choosing.</p>
            </div>
            <div class="experience-card">
                <h4>Compliance-first pace</h4>
                <p>Your coverage and key details are organized and easy to review whenever you need—no pressure, no surprises.</p>
            </div>
            <div class="experience-card">
                <h4>Carrier-agnostic curation</h4>
                <p>We evaluate the right carriers for your situation—no single-company bias—then align to your cash flow and runway.</p>
            </div>
            <div class="experience-card">
                <h4>Ongoing stewardship</h4>
                <p>Your plan stays updated as income, debts, and goals shift. You always know what’s protected and what’s next.</p>
            </div>
        </div>
    </section>

    <!-- TRUST STRIP -->
    <section class="section">
        <div class="section-head">
            <div class="section-sub" style="margin-bottom:12px;">Trusted by clients who need clarity, protection, and clean execution.</div>
        </div>
        <div class="trust-strip">
            <div class="stat">
                <span class="value">Faith Based</span>
                <span class="label">Client-first, relationship-driven planning</span>
            </div>
            <div class="stat">
                <span class="value">Timely</span>
                <span class="label">Structured planning without being rushed</span>
            </div>
            <div class="stat">
                <span class="value">Alignment</span>
                <span class="label">Built to work alongside your broader financial picture</span>
            </div>
            <div class="stat">
                <span class="value">Structured</span>
                <span class="label">Built with structure, clarity, and accountability</span>
            </div>
        </div>
    </section>

    <!-- PROBLEM / OUTCOME -->
    <section class="section">
        <div class="section-head">
            <h2 class="section-title">What you gain</h2>
            <p class="section-sub">Three outcomes every client cares about—delivered without jargon.</p>
        </div>
        <div class="grid-3">
            <div class="card">
                <p class="pill-title">Clarity</p>
                <h3>Know exactly where you stand.</h3>
                <p>We map accounts, obligations, and coverage so you see gaps and priorities on one page.</p>
            </div>
            <div class="card">
                <p class="pill-title">Protection</p>
                <h3>Protect income and family stability.</h3>
                <p>Right-size life, disability, and health strategies around your real cash flow and risks.</p>
            </div>
            <div class="card">
                <p class="pill-title">Legacy</p>
                <h3>Keep what you build intact.</h3>
                <p>Beneficiaries, ownership, and transfer structure that survive surprises—not just good intentions.</p>
            </div>
        </div>
    </section>

    <!-- SERVICES / VALUE -->
    <section class="section">
        <div class="section-head">
            <h2 class="section-title">How we deliver</h2>
            <p class="section-sub">Focused, professional-led planning and protection.</p>
        </div>
        <div class="grid-3">
            <div class="card">
                <p class="pill-title">Planning</p>
                <h3>Financial organization</h3>
                <p>Clear inventory, priorities, and a documented action path so you stay in control.</p>
            </div>
            <div class="card">
                <p class="pill-title">Protection</p>
                <h3>Coverage built on reality</h3>
                <p>We tailor life/disability/health to your income, debts, dependents, and runway.</p>
            </div>
            <div class="card">
                <p class="pill-title">Legacy</p>
                <h3>Long-term structure</h3>
                <p>Ownership, beneficiaries, and coordination with your estate/legal pros for clean transfer.</p>
            </div>
        </div>
    </section>

    <!-- PROCESS -->
    <section class="section">
        <div class="section-head">
            <h2 class="section-title">Simple, fast process</h2>
            <p class="section-sub">Three steps that get you protected without the noise.</p>
        </div>
        <div class="process">
            <div class="process-card">
                <div class="badge-num">1</div>
                <h4>Assess</h4>
                <p>We gather your facts, goals, and risks in one clean assessment.</p>
            </div>
            <div class="process-card">
                <div class="badge-num">2</div>
                <h4>Design</h4>
                <p>We walk you through your options clearly, so you understand what you’re choosing and why it makes sense.</p>
            </div>
            <div class="process-card">
                <div class="badge-num">3</div>
                <h4>Implement</h4>
                <p>We place the coverage, document it, and keep it updated, so you stay protected as life moves.</p>
            </div>
        </div>
        <div class="cta-row" style="margin-top:16px; justify-content:center;">
            <a asp-controller="RiskAssessment" asp-action="Index" class="btn btn-primary" data-cta="hero_start_assessment" data-lead-trigger="true" data-lead-interest="assessment">Start Assessment</a>
            <a href="@bookingLink" target="_blank" rel="noopener" class="btn btn-secondary" data-cta="footer_book_call">Book a Call</a>
        </div>
    </section>

    <!-- FAQ -->
    <section class="section">
        <div class="section-head">
            <h2 class="section-title">FAQs</h2>
        </div>
        <div class="faq">
            <div class="card">
                <h4>What happens in the assessment?</h4>
                <p>We collect your goals, income, obligations, and current coverage. You get a clear gap analysis and next steps.</p>
            </div>
            <div class="card">
                <h4>Is this just insurance?</h4>
                <p>No. We start with organization and risk clarity, then tailor coverage. Planning and protection move together.</p>
            </div>
            <div class="card">
                <h4>Is there pressure to buy?</h4>
                <p>No. You see options, tradeoffs, and costs. You choose the path that makes the most sense for you.</p>
            </div>
            <div class="card">
                <h4>How fast do I see a plan?</h4>
                <p>You’ll receive your plan in a timely, well-structured way, without anything being rushed or overlooked.</p>
            </div>
            <div class="card">
                <h4>Can you work with my CPA/attorney?</h4>
                <p>Yes. We structure everything clearly so your CPA or attorney can review it, and we help keep everything aligned so nothing conflicts or gets missed.</p>
            </div>
        </div>
    </section>

    <!-- FINAL CTA BANNER -->
    <section class="section shell-dark cta-banner">
        <h3>Ready to lock in clarity and protection?</h3>
        <p>Start with a clean assessment or talk with a licensed professional.</p>
        <div class="cta-row" style="justify-content:center;">
            <a asp-controller="RiskAssessment" asp-action="Index" class="btn btn-primary" data-cta="hero_start_assessment" data-lead-trigger="true" data-lead-interest="assessment">Start Assessment</a>
            <a href="@bookingLink" target="_blank" rel="noopener" class="btn btn-secondary" data-cta="footer_book_call">Book a Call</a>
        </div>
    </section>

</div>
