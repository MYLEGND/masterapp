name: Temporary LEGEND terminal retirement repair

on:
  push:
    branches:
      - fix/legend-terminal-retirement-reasons-20260824

permissions:
  contents: write

jobs:
  repair-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Apply narrow production repair
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected exactly 1 match, found {count}")
              return text.replace(old, new, 1)

          def replace_count(text, old, new, expected, label):
              count = text.count(old)
              if count != expected:
                  raise SystemExit(f"{label}: expected {expected} matches, found {count}")
              return text.replace(old, new)

          def sub_once(text, pattern, replacement, label, flags=0):
              updated, count = re.subn(pattern, replacement, text, count=1, flags=flags)
              if count != 1:
                  raise SystemExit(f"{label}: expected exactly 1 regex match, found {count}")
              return updated

          # -----------------------------------------------------------------
          # 1) One durable work authority: terminal failure becomes Retired.
          #    Retired remains an immutable historical tombstone, is never
          #    claimable, does not block convergence, and prevents reseeding.
          # -----------------------------------------------------------------
          path = Path('Infrastructure/Messaging/LegendConnectHistoricalReevaluationWork.cs')
          text = path.read_text()

          text = replace_once(
              text,
              '    internal const string Failed = "Failed";\n',
              '    internal const string Failed = "Failed";\n'
              '    // Terminal execution tombstone. Retired work is deleted from the active\n'
              '    // scheduler after the configured retry ceiling while its immutable work\n'
              '    // identity, attempts, and exact failure remain in this same historical table.\n'
              '    internal const string Retired = "Retired";\n',
              'add Retired state')

          retirement_methods = r'''
    /// <summary>
    /// Converts terminal historical failures into non-executable tombstones.
    /// The row is deliberately retained in this same durable authority so its
    /// identity, retry count and exact failure remain auditable and, critically,
    /// so a phase seeder cannot rediscover and recreate the poisoned identity.
    /// No curriculum/evidence row is rewritten and no replay is restarted.
    /// </summary>
    internal async Task<int> RetireTerminalFailuresAsync(
        int evaluatorVersion,
        string phase,
        CancellationToken cancellationToken = default)
    {
        var now = DateTime.UtcNow;
        var terminal = WorkFor(evaluatorVersion, phase)
            .Where(item => item.WorkKind != SeedWorkKind &&
                item.ProcessingState == Failed &&
                item.AttemptCount >= MaximumAttempts);

        var retired = 0;
        if (_db.Database.IsRelational())
        {
            retired = await terminal.ExecuteUpdateAsync(setters => setters
                .SetProperty(item => item.ProcessingState, Retired)
                .SetProperty(item => item.LeaseOwner, (string?)null)
                .SetProperty(item => item.LeaseToken, (Guid?)null)
                .SetProperty(item => item.LeaseExpiresUtc, (DateTime?)null)
                .SetProperty(item => item.UpdatedUtc, now), cancellationToken);
        }
        else
        {
            var items = await terminal.ToListAsync(cancellationToken);
            foreach (var item in items)
            {
                item.ProcessingState = Retired;
                item.LeaseOwner = null;
                item.LeaseToken = null;
                item.LeaseExpiresUtc = null;
                item.UpdatedUtc = now;
            }
            if (items.Count > 0)
                await _db.SaveChangesAsync(cancellationToken);
            retired = items.Count;
        }

        return retired + await RetireBlockedDependentsAsync(
            evaluatorVersion,
            phase,
            cancellationToken);
    }

    private async Task<int> RetireBlockedDependentsAsync(
        int evaluatorVersion,
        string phase,
        CancellationToken cancellationToken)
    {
        IQueryable<LegendHistoricalReevaluationWorkItem>? blocked = null;

        if (string.Equals(
                phase,
                LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
                StringComparison.Ordinal))
        {
            blocked = WorkFor(evaluatorVersion, phase)
                .Where(item => item.WorkKind == DerivationLedgerWorkKind &&
                    item.ProcessingState == Pending &&
                    _db.Set<LegendHistoricalReevaluationWorkItem>().Any(parent =>
                        parent.EvaluatorVersion == evaluatorVersion &&
                        parent.Phase == phase &&
                        parent.WorkKind == CanonicalWorkKind &&
                        parent.ProcessingState == Retired &&
                        parent.SubjectId == item.SubjectId &&
                        parent.SubjectScope == item.SubjectScope));
        }
        else if (string.Equals(phase, FounderCurriculumPhase, StringComparison.Ordinal))
        {
            blocked = WorkFor(evaluatorVersion, phase)
                .Where(item => item.ProcessingState == Pending &&
                    ((item.WorkKind == FounderManifestSemanticRelationWorkKind &&
                      _db.Set<LegendHistoricalReevaluationWorkItem>().Any(parent =>
                          parent.EvaluatorVersion == evaluatorVersion &&
                          parent.Phase == FounderCurriculumPhase &&
                          parent.SubjectId == item.SubjectId &&
                          parent.WorkKind == FounderManifestFamilyWorkKind &&
                          parent.ProcessingState == Retired)) ||
                     (item.WorkKind == DerivationLedgerWorkKind &&
                      _db.Set<LegendHistoricalReevaluationWorkItem>().Any(parent =>
                          parent.EvaluatorVersion == evaluatorVersion &&
                          parent.Phase == FounderCurriculumPhase &&
                          parent.SubjectId == item.SubjectId &&
                          (parent.WorkKind == FounderManifestFamilyWorkKind ||
                           parent.WorkKind == FounderManifestSemanticRelationWorkKind) &&
                          parent.ProcessingState == Retired))));
        }

        if (blocked is null)
            return 0;

        var now = DateTime.UtcNow;
        const string code = "historical_reevaluation_prerequisite_retired";
        const string message =
            "A required upstream work identity reached its retry ceiling and was retired; this dependent identity was retired without execution.";

        if (_db.Database.IsRelational())
        {
            return await blocked.ExecuteUpdateAsync(setters => setters
                .SetProperty(item => item.ProcessingState, Retired)
                .SetProperty(item => item.LeaseOwner, (string?)null)
                .SetProperty(item => item.LeaseToken, (Guid?)null)
                .SetProperty(item => item.LeaseExpiresUtc, (DateTime?)null)
                .SetProperty(item => item.LastErrorCode, code)
                .SetProperty(item => item.LastErrorMessage, message)
                .SetProperty(item => item.UpdatedUtc, now), cancellationToken);
        }

        var items = await blocked.ToListAsync(cancellationToken);
        foreach (var item in items)
        {
            item.ProcessingState = Retired;
            item.LeaseOwner = null;
            item.LeaseToken = null;
            item.LeaseExpiresUtc = null;
            item.LastErrorCode = code;
            item.LastErrorMessage = message;
            item.UpdatedUtc = now;
        }
        if (items.Count > 0)
            await _db.SaveChangesAsync(cancellationToken);
        return items.Count;
    }

'''
          marker = '    /// <summary>\n    /// Reclaims expired durable leases before any new claim.'
          text = replace_once(text, marker, retirement_methods + marker, 'insert retirement authority')

          text = replace_once(
              text,
              '    {\n        var now = DateTime.UtcNow;\n        var query = WorkFor(evaluatorVersion, phase)\n            .Where(item => item.ProcessingState == Processing &&\n',
              '    {\n        await RetireTerminalFailuresAsync(evaluatorVersion, phase, cancellationToken);\n        var now = DateTime.UtcNow;\n        var query = WorkFor(evaluatorVersion, phase)\n            .Where(item => item.ProcessingState == Processing &&\n',
              'retire historical failures before lease recovery')

          family_old = '''            if (existing.TryGetValue(scope, out var prior))
            {
                // Explicit Founder resubmission is the only supported way to
                // retry a terminal manifest. Preserve canonical rows and
                // reopen only its durable execution identity.
                if (prior.ProcessingState == Failed)
                {
                    prior.ProcessingState = Pending;
                    prior.LeaseOwner = null;
                    prior.LeaseToken = null;
                    prior.LeaseExpiresUtc = null;
                    prior.AttemptCount = 0;
                    prior.LastErrorCode = null;
                    prior.LastErrorMessage = null;
                    prior.UpdatedUtc = now;
                    changed = true;
                }
                continue;
            }
'''
          relation_old = '''            if (existing.TryGetValue(scope, out var prior))
            {
                if (prior.ProcessingState == Failed)
                {
                    prior.ProcessingState = Pending;
                    prior.LeaseOwner = null;
                    prior.LeaseToken = null;
                    prior.LeaseExpiresUtc = null;
                    prior.AttemptCount = 0;
                    prior.LastErrorCode = null;
                    prior.LastErrorMessage = null;
                    prior.UpdatedUtc = now;
                    changed = true;
                }
                continue;
            }
'''
          family_new = '''            // Existing deterministic identities are authoritative in every terminal
            // state. Ordinary worker seeding must never reset attempts or resurrect a
            // failed/retired identity. A corrected Founder submission receives its own
            // accepted manifest identity; a later evaluator receives its own versioned work.
            if (existing.ContainsKey(scope))
                continue;
'''
          text = replace_once(text, family_old, family_new, 'stop Founder family resurrection')
          text = replace_count(text, relation_old, '            if (existing.ContainsKey(scope))\n                continue;\n', 2, 'stop relation/ledger resurrection')

          text = replace_once(
              text,
              '''    internal async Task FailAsync(
        LegendHistoricalReevaluationWorkClaim claim,
        string errorCode,
        CancellationToken cancellationToken = default)
    {
        var now = DateTime.UtcNow;
''',
              '''    internal async Task FailAsync(
        LegendHistoricalReevaluationWorkClaim claim,
        string errorCode,
        CancellationToken cancellationToken = default,
        string? errorMessage = null)
    {
        var now = DateTime.UtcNow;
        var failureMessage = NormalizeFailureMessage(errorMessage);
''',
              'extend failure detail contract')
          text = replace_count(
              text,
              'item => item.AttemptCount >= MaximumAttempts ? Failed : Pending',
              'item => item.AttemptCount >= MaximumAttempts && item.WorkKind != SeedWorkKind ? Retired :\n                        item.AttemptCount >= MaximumAttempts ? Failed : Pending',
              1,
              'retire relational terminal failure')
          text = replace_once(
              text,
              '        item.ProcessingState = item.AttemptCount >= MaximumAttempts ? Failed : Pending;\n',
              '        item.ProcessingState = item.AttemptCount >= MaximumAttempts && item.WorkKind != SeedWorkKind\n            ? Retired\n            : item.AttemptCount >= MaximumAttempts ? Failed : Pending;\n',
              'retire in-memory terminal failure')
          text = replace_count(
              text,
              '                    "The canonical evaluator failed without creating a replacement authority.")',
              '                    failureMessage)',
              1,
              'persist exact relational failure detail')
          text = replace_once(
              text,
              '        item.LastErrorMessage = "The canonical evaluator failed without creating a replacement authority.";\n',
              '        item.LastErrorMessage = failureMessage;\n',
              'persist exact in-memory failure detail')

          normalize_failure = '''
    private static string NormalizeFailureMessage(string? value)
    {
        const string fallback =
            "The canonical evaluator failed without exposing a more specific exception detail.";
        var normalized = string.IsNullOrWhiteSpace(value) ? fallback : value.Trim();
        return normalized.Length <= 1000 ? normalized : normalized[..1000];
    }

'''
          advance_marker = '    /// <summary>\n    /// Advances a phase only under a serializable relational barrier:'
          text = replace_once(text, advance_marker, normalize_failure + advance_marker, 'insert failure detail normalizer')

          text = replace_once(
              text,
              '''        var now = DateTime.UtcNow;
        var work = WorkFor(evaluatorVersion, phase);
''',
              '''        await RetireTerminalFailuresAsync(evaluatorVersion, phase, cancellationToken);
        var now = DateTime.UtcNow;
        var work = WorkFor(evaluatorVersion, phase);
''',
              'retire before phase barrier')

          text = replace_once(
              text,
              '''        var now = DateTime.UtcNow;
        var work = WorkFor(evaluatorVersion, phase).AsNoTracking()
            .Where(item => item.WorkKind == CanonicalWorkKind ||
                item.WorkKind == DerivationLedgerWorkKind);
''',
              '''        await RetireTerminalFailuresAsync(evaluatorVersion, phase, cancellationToken);
        var now = DateTime.UtcNow;
        var work = WorkFor(evaluatorVersion, phase).AsNoTracking()
            .Where(item => (item.WorkKind == CanonicalWorkKind ||
                item.WorkKind == DerivationLedgerWorkKind) &&
                item.ProcessingState != Retired);
''',
              'exclude retired tombstones from active convergence status')

          text = replace_count(
              text,
              'canonical.ProcessingState != Completed',
              'canonical.ProcessingState != Completed && canonical.ProcessingState != Retired',
              text.count('canonical.ProcessingState != Completed'),
              'allow source ledger drain past retired canonical')
          raw_count = text.count('[canonical].[ProcessingState] <> {Completed}')
          if raw_count < 1:
              raise SystemExit('raw canonical drain gate: expected at least one match')
          text = text.replace(
              '[canonical].[ProcessingState] <> {Completed}',
              '[canonical].[ProcessingState] NOT IN ({Completed}, {Retired})')

          text = text.replace(
              'await FailAsync(claim, FailureCode(exception), cancellationToken);',
              'await FailAsync(claim, FailureCode(exception), cancellationToken, exception.ToString());')

          path.write_text(text)

          # -----------------------------------------------------------------
          # 2) Founder manifest projection: retain exact failure and never
          #    re-admit a current-evaluator terminal receipt through seeding.
          # -----------------------------------------------------------------
          path = Path('Infrastructure/Messaging/LegendConnectCurriculumManifestProcessing.cs')
          text = path.read_text()
          text = replace_once(
              text,
              '''            await _durableWork.FailAsync(
                claim,
                "founder_manifest_family_failure",
                CancellationToken.None);
''',
              '''            await _durableWork.FailAsync(
                claim,
                "founder_manifest_family_failure",
                CancellationToken.None,
                exception.ToString());
''',
              'persist Founder exception detail')

          text = replace_once(
              text,
              '''                (item.ProcessingState != "Failed" ||
                 (item.LastErrorCode != "curriculum_manifest_payload_invalid" &&
                  item.LastErrorCode != "curriculum_manifest_payload_mismatch")) &&
                (item.ProcessingState != "Completed" ||
                 item.CompletedLanguageIntelligenceEvaluatorVersion < evaluatorVersion))
''',
              '''                (item.ProcessingState != "Failed" ||
                 (item.LastErrorCode != "curriculum_manifest_payload_invalid" &&
                  item.LastErrorCode != "curriculum_manifest_payload_mismatch" &&
                  item.TargetLanguageIntelligenceEvaluatorVersion < evaluatorVersion)) &&
                (item.ProcessingState != "Completed" ||
                 item.CompletedLanguageIntelligenceEvaluatorVersion < evaluatorVersion))
''',
              'prevent current evaluator failed manifest reseeding')

          text = replace_once(
              text,
              '        if (!result.Succeeded)\n            throw new InvalidOperationException(result.ErrorCode ?? "curriculum_family_processing_rejected");\n',
              '        if (!result.Succeeded)\n            throw new InvalidOperationException(\n                $"{result.ErrorCode ?? "curriculum_family_processing_rejected"}: " +\n                (result.Message ?? "The canonical curriculum authority rejected the family without additional detail."));\n',
              'preserve curriculum rejection detail')

          text = replace_once(
              text,
              '        var failed = children.FirstOrDefault(item => item.ProcessingState == "Failed");\n',
              '        var failed = children.FirstOrDefault(item =>\n            item.ProcessingState == "Failed" ||\n            item.ProcessingState == LegendConnectHistoricalReevaluationWorkAuthority.Retired);\n',
              'project retired child as terminal manifest failure')
          path.write_text(text)

          # -----------------------------------------------------------------
          # 3) Chat failure projection: no generic fallback. Surface the exact
          #    native reason/authority summary and concrete provider failure.
          # -----------------------------------------------------------------
          path = Path('AgentPortal/Services/LegendFounderAiConversationService.cs')
          text = path.read_text()
          text = replace_once(
              text,
              '        LegendConnectNativeInferenceSnapshot? nativeInference = null;\n',
              '        LegendConnectNativeInferenceSnapshot? nativeInference = null;\n        string? nativeFailureDetail = null;\n',
              'track native exception detail')

          text = replace_once(
              text,
              '''                _logger.LogWarning(
                    exception,
                    "LEGEND native conversational inference was unavailable; escalating without a native answer.");
''',
              '''                nativeFailureDetail = exception.ToString();
                _logger.LogWarning(
                    exception,
                    "LEGEND native conversational inference was unavailable; escalating without a native answer.");
''',
              'capture native exception')

          text = replace_once(
              text,
              '''            return NativeInferenceUnavailableResponse(
                mode,
                nativeInference);
''',
              '''            return NativeInferenceUnavailableResponse(
                mode,
                nativeInference,
                nativeFailureDetail);
''',
              'native fail-closed detail')

          text = replace_once(
              text,
              '        if (string.IsNullOrWhiteSpace(apiKey))\n            return NativeInferenceUnavailableResponse(mode, nativeInference);\n',
              '''        if (string.IsNullOrWhiteSpace(apiKey))
            return NativeInferenceUnavailableResponse(
                mode,
                nativeInference,
                nativeFailureDetail,
                "provider_api_key_unavailable",
                "The external reasoning provider is not configured for this deployment.");
''',
              'provider configuration detail')

          text = replace_once(
              text,
              '''                if (responseDocument is null)
                {
                    return NativeInferenceUnavailableResponse(mode, nativeInference);
                }
''',
              '''                if (responseDocument is null)
                {
                    return NativeInferenceUnavailableResponse(
                        mode,
                        nativeInference,
                        nativeFailureDetail,
                        "provider_no_response",
                        "The provider request completed without a usable response document.");
                }
''',
              'provider null response detail')

          text = replace_once(
              text,
              '''            return NativeInferenceUnavailableResponse(mode, nativeInference);
        }
        catch (OperationCanceledException)
            when (!cancellationToken.IsCancellationRequested)
        {
            return NativeInferenceUnavailableResponse(mode, nativeInference);
        }
        catch (HttpRequestException exception)
        {
            _logger.LogWarning(
                exception,
                "LEGEND Founder AI provider transport failed.");

            return NativeInferenceUnavailableResponse(mode, nativeInference);
        }
        catch (JsonException exception)
        {
            _logger.LogWarning(
                exception,
                "LEGEND Founder AI received invalid provider JSON.");

            return NativeInferenceUnavailableResponse(mode, nativeInference);
        }
''',
              '''            return NativeInferenceUnavailableResponse(
                mode,
                nativeInference,
                nativeFailureDetail,
                $"provider_http_{exception.StatusCode}",
                $"{exception.ProviderError} ClientRequestId={exception.ClientRequestId}; ProviderRequestId={exception.ProviderRequestId ?? "unavailable"}.");
        }
        catch (OperationCanceledException exception)
            when (!cancellationToken.IsCancellationRequested)
        {
            return NativeInferenceUnavailableResponse(
                mode,
                nativeInference,
                nativeFailureDetail,
                "provider_timeout",
                exception.Message);
        }
        catch (HttpRequestException exception)
        {
            _logger.LogWarning(
                exception,
                "LEGEND Founder AI provider transport failed.");

            return NativeInferenceUnavailableResponse(
                mode,
                nativeInference,
                nativeFailureDetail,
                "provider_transport_failure",
                exception.Message);
        }
        catch (JsonException exception)
        {
            _logger.LogWarning(
                exception,
                "LEGEND Founder AI received invalid provider JSON.");

            return NativeInferenceUnavailableResponse(
                mode,
                nativeInference,
                nativeFailureDetail,
                "provider_invalid_json",
                exception.Message);
        }
''',
              'provider catch diagnostics')

          text = replace_once(
              text,
              '''            throw new LegendFounderAiProviderException(
                (int)response.StatusCode,
                clientRequestId,
                providerRequestId);
''',
              '''            throw new LegendFounderAiProviderException(
                (int)response.StatusCode,
                clientRequestId,
                providerRequestId,
                errorBody);
''',
              'carry provider error body')

          helper_pattern = re.compile(
              r'    private static LegendFounderAiChatResponse NativeInferenceUnavailableResponse\(\n.*?\n    private static string DescribeFounderToolCall',
              re.S)
          helper_replacement = '''    private static LegendFounderAiChatResponse NativeInferenceUnavailableResponse(
        string mode,
        LegendConnectNativeInferenceSnapshot? nativeInference,
        string? nativeFailureDetail = null,
        string? providerFailureCode = null,
        string? providerFailureDetail = null)
    {
        var nativeReasonCode = string.IsNullOrWhiteSpace(nativeInference?.ReasonCode)
            ? nativeInference is null ? "native_inference_unavailable" : "native_inference_unsupported"
            : nativeInference.ReasonCode.Trim();
        var nativeDetail = !string.IsNullOrWhiteSpace(nativeFailureDetail)
            ? NormalizeFailureDetail(nativeFailureDetail)
            : !string.IsNullOrWhiteSpace(nativeInference?.AuthoritySummary)
                ? nativeInference.AuthoritySummary.Trim()
                : "The native authority returned no additional failure detail.";
        var evidenceCount = nativeInference?.EvidenceCount ?? 0;
        var escalationState = nativeInference?.RequiresEscalation == true
            ? "required"
            : "not_permitted";

        var providerCode = string.IsNullOrWhiteSpace(providerFailureCode)
            ? nativeInference?.RequiresEscalation == true
                ? "provider_unavailable_without_detail"
                : "provider_not_attempted"
            : providerFailureCode.Trim();
        var providerDetail = string.IsNullOrWhiteSpace(providerFailureCode)
            ? nativeInference?.RequiresEscalation == true
                ? "The escalation path did not expose a provider-specific failure detail."
                : "The governed native result did not permit external escalation."
            : NormalizeFailureDetail(providerFailureDetail);

        return new LegendFounderAiChatResponse(
            true,
            mode,
            $"LEGEND could not complete this response. " +
            $"NativeFailure={nativeReasonCode}; NativeDetail={nativeDetail}; " +
            $"EvidenceCount={evidenceCount}; Escalation={escalationState}; " +
            $"ProviderFailure={providerCode}; ProviderDetail={providerDetail}",
            null);
    }

    private static string NormalizeFailureDetail(string? value)
    {
        var normalized = string.IsNullOrWhiteSpace(value)
            ? "No additional detail was supplied."
            : value.Trim();
        return normalized.Length <= 1000 ? normalized : normalized[..1000];
    }

    private static string DescribeFounderToolCall'''
          text, count = helper_pattern.subn(helper_replacement, text, count=1)
          if count != 1:
              raise SystemExit(f'replace generic native fallback: expected 1 match, found {count}')

          text = replace_once(
              text,
              '''        public LegendFounderAiProviderException(
            int statusCode,
            string clientRequestId,
            string? providerRequestId)
            : base(
                $"Legend Founder AI provider returned HTTP {statusCode}.")
        {
            StatusCode = statusCode;
            ClientRequestId = clientRequestId;
            ProviderRequestId = providerRequestId;
        }
''',
              '''        public LegendFounderAiProviderException(
            int statusCode,
            string clientRequestId,
            string? providerRequestId,
            string providerError)
            : base(
                $"Legend Founder AI provider returned HTTP {statusCode}.")
        {
            StatusCode = statusCode;
            ClientRequestId = clientRequestId;
            ProviderRequestId = providerRequestId;
            ProviderError = NormalizeFailureDetail(providerError);
        }
''',
              'extend provider exception')
          text = replace_once(
              text,
              '        public string? ProviderRequestId { get; }\n',
              '        public string? ProviderRequestId { get; }\n\n        public string ProviderError { get; }\n',
              'provider error property')
          path.write_text(text)

          # -----------------------------------------------------------------
          # 4) Regression coverage.
          # -----------------------------------------------------------------
          path = Path('AgentPortal.Tests/LegendConnectHistoricalReevaluationWorkTests.cs')
          text = path.read_text()
          retry_pattern = re.compile(
              r'    \[Fact\]\n    public async Task RetryAccounting_IsDeterministic_AndTerminalFailureBlocksThePhase\(\)\n    \{.*?\n    \}\n\n    \[Fact\]\n    public async Task SeedingAndCompletedWork_AreIdempotentAcrossAuthorityRestart',
              re.S)
          retry_replacement = '''    [Fact]
    public async Task RetryAccounting_IsDeterministic_AndTerminalFailureRetiresWithoutBlockingThePhase()
    {
        await using var fixture = await Fixture.CreateAsync(maximumAttempts: 2);
        await fixture.StartAsync(EvaluatorVersion);
        AddSourceFamily(fixture.Db, "en", "only", 401);
        await fixture.Db.SaveChangesAsync();
        await fixture.Work.SeedNextBatchAsync(
            EvaluatorVersion,
            LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
            "test-seeder");

        var first = Assert.IsType<LegendHistoricalReevaluationWorkClaim>(
            await fixture.Work.TryClaimNextAsync(
                EvaluatorVersion,
                LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
                "worker-a"));
        await fixture.Work.FailAsync(first, "test_failure", errorMessage: "first exact failure");
        var afterFirst = await fixture.Db.LegendHistoricalReevaluationWorkItems
            .SingleAsync(item => item.Id == first.WorkItemId);
        Assert.Equal(1, afterFirst.AttemptCount);
        Assert.Equal("Pending", afterFirst.ProcessingState);

        var second = Assert.IsType<LegendHistoricalReevaluationWorkClaim>(
            await fixture.Work.TryClaimNextAsync(
                EvaluatorVersion,
                LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
                "worker-b"));
        await fixture.Work.FailAsync(second, "test_failure", errorMessage: "second exact terminal failure");
        var afterSecond = await fixture.Db.LegendHistoricalReevaluationWorkItems
            .SingleAsync(item => item.Id == first.WorkItemId);
        Assert.Equal(2, afterSecond.AttemptCount);
        Assert.Equal(LegendConnectHistoricalReevaluationWorkAuthority.Retired, afterSecond.ProcessingState);
        Assert.Equal("test_failure", afterSecond.LastErrorCode);
        Assert.Equal("second exact terminal failure", afterSecond.LastErrorMessage);
        Assert.Null(await fixture.Work.TryClaimNextAsync(
            EvaluatorVersion,
            LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
            "worker-c"));
        Assert.True(await fixture.Work.TryAdvancePhaseAsync(
            EvaluatorVersion,
            LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies));
    }

    [Fact]
    public async Task HistoricalTerminalFailure_IsRetiredInPlace_AndCannotBeReseeded()
    {
        await using var fixture = await Fixture.CreateAsync(maximumAttempts: 2);
        await fixture.StartAsync(EvaluatorVersion);
        AddSourceFamily(fixture.Db, "en", "historical", 402);
        await fixture.Db.SaveChangesAsync();
        await fixture.Work.SeedNextBatchAsync(
            EvaluatorVersion,
            LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
            "test-seeder");

        var historical = await fixture.Db.LegendHistoricalReevaluationWorkItems
            .SingleAsync(item => item.WorkKind == "Canonical");
        historical.ProcessingState = "Failed";
        historical.AttemptCount = 2;
        historical.LastErrorCode = "historic_failure";
        historical.LastErrorMessage = "preserved historical detail";
        await fixture.Db.SaveChangesAsync();

        var seedResult = await fixture.Work.SeedNextBatchAsync(
            EvaluatorVersion,
            LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
            "test-seeder-2");
        var retired = await fixture.Db.LegendHistoricalReevaluationWorkItems
            .SingleAsync(item => item.Id == historical.Id);

        Assert.Equal(LegendConnectHistoricalReevaluationWorkAuthority.Retired, retired.ProcessingState);
        Assert.Equal(2, retired.AttemptCount);
        Assert.Equal("historic_failure", retired.LastErrorCode);
        Assert.Equal("preserved historical detail", retired.LastErrorMessage);
        Assert.Equal(0, seedResult.SeededCount);
        Assert.Null(await fixture.Work.TryClaimNextAsync(
            EvaluatorVersion,
            LegendConnectLanguageIntelligenceReevaluationPhases.SourceFamilies,
            "worker-after-retirement"));
    }

    [Fact]
    public async Task FounderSeed_DoesNotResurrectRetiredDeterministicIdentity()
    {
        await using var fixture = await Fixture.CreateAsync(maximumAttempts: 2);
        var manifestId = Guid.NewGuid();
        var seeds = new[]
        {
            new LegendFounderManifestFamilyWorkSeed(
                "family.test",
                "dependency:test",
                "canonical:test")
        };

        Assert.True(await fixture.Work.SeedFounderManifestFamiliesAsync(
            EvaluatorVersion,
            manifestId,
            seeds));
        var work = await fixture.Db.LegendHistoricalReevaluationWorkItems.SingleAsync();
        work.ProcessingState = LegendConnectHistoricalReevaluationWorkAuthority.Retired;
        work.AttemptCount = 2;
        work.LastErrorCode = "terminal";
        work.LastErrorMessage = "do not resurrect";
        await fixture.Db.SaveChangesAsync();

        Assert.False(await fixture.Work.SeedFounderManifestFamiliesAsync(
            EvaluatorVersion,
            manifestId,
            seeds));
        var preserved = await fixture.Db.LegendHistoricalReevaluationWorkItems.SingleAsync();
        Assert.Equal(LegendConnectHistoricalReevaluationWorkAuthority.Retired, preserved.ProcessingState);
        Assert.Equal(2, preserved.AttemptCount);
        Assert.Equal("terminal", preserved.LastErrorCode);
        Assert.Equal("do not resurrect", preserved.LastErrorMessage);
    }

    [Fact]
    public async Task SeedingAndCompletedWork_AreIdempotentAcrossAuthorityRestart'''
          text, count = retry_pattern.subn(retry_replacement, text, count=1)
          if count != 1:
              raise SystemExit(f'update retry regression: expected 1 match, found {count}')
          path.write_text(text)

          path = Path('AgentPortal.Tests/LegendFounderAiConversationRoutingTests.cs')
          text = path.read_text()
          text = replace_once(text, 'using AgentPortal.Services;\n', 'using AgentPortal.Services;\nusing Domain.Messaging;\n', 'add native snapshot import')
          insert = '''

    [Fact]
    public void NativeFailureResponse_ExposesGovernedReasonAndProviderFailureDetail()
    {
        var method = typeof(LegendFounderAiConversationService)
            .GetMethod("NativeInferenceUnavailableResponse", BindingFlags.NonPublic | BindingFlags.Static);
        Assert.NotNull(method);
        var snapshot = new LegendConnectNativeInferenceSnapshot(
            false,
            0.42m,
            null,
            "semantic_transition_not_production_eligible",
            9,
            "The matching transition exists but has not crossed the production eligibility gate.",
            true);

        var response = Assert.IsType<LegendFounderAiChatResponse>(method!.Invoke(
            null,
            new object?[]
            {
                "legend",
                snapshot,
                null,
                "provider_http_429",
                "Provider reported no remaining credits."
            }));

        Assert.True(response.Succeeded);
        Assert.Contains("semantic_transition_not_production_eligible", response.Message);
        Assert.Contains("production eligibility gate", response.Message);
        Assert.Contains("EvidenceCount=9", response.Message);
        Assert.Contains("provider_http_429", response.Message);
        Assert.Contains("no remaining credits", response.Message);
        Assert.DoesNotContain("does not yet have enough governed evidence", response.Message);
    }
'''
          idx = text.rfind('\n}')
          if idx < 0:
              raise SystemExit('routing test closing brace not found')
          text = text[:idx] + insert + text[idx:]
          path.write_text(text)

          # Hard fail if the two known anti-patterns survived.
          historical = Path('Infrastructure/Messaging/LegendConnectHistoricalReevaluationWork.cs').read_text()
          manifest = Path('Infrastructure/Messaging/LegendConnectCurriculumManifestProcessing.cs').read_text()
          conversation = Path('AgentPortal/Services/LegendFounderAiConversationService.cs').read_text()
          if 'prior.ProcessingState == Failed' in historical:
              raise SystemExit('ordinary Founder seeding still reopens Failed work')
          if 'does not yet have enough governed evidence to answer this directly' in conversation:
              raise SystemExit('generic chat fallback survived repair')
          if 'item.AttemptCount >= MaximumAttempts ? Failed : Pending' in historical:
              raise SystemExit('terminal work still remains executable Failed state')
          if 'TargetLanguageIntelligenceEvaluatorVersion < evaluatorVersion' not in manifest:
              raise SystemExit('current-evaluator failed manifest reseed guard missing')
          PY

      - name: Verify patch shape
        shell: bash
        run: |
          git diff --check
          git diff --stat
          git status --short

      - name: Restore
        run: dotnet restore AgentPortal.Tests/AgentPortal.Tests.csproj

      - name: Build production
        run: dotnet build AgentPortal/AgentPortal.csproj --configuration Release --no-restore

      - name: Build tests
        run: dotnet build AgentPortal.Tests/AgentPortal.Tests.csproj --configuration Release --no-restore

      - name: Run focused retirement and failure-reason regression
        shell: bash
        run: |
          dotnet test AgentPortal.Tests/AgentPortal.Tests.csproj \
            --configuration Release --no-build \
            --filter 'FullyQualifiedName~LegendConnectHistoricalReevaluationWorkTests|FullyQualifiedName~LegendFounderAiConversationRoutingTests' \
            --logger 'console;verbosity=normal'

      - name: Run full AgentPortal regression
        run: dotnet test AgentPortal.Tests/AgentPortal.Tests.csproj --configuration Release --no-build --logger 'console;verbosity=normal'

      - name: Commit only after all validation is green
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/workflows/temporary-legend-terminal-retirement-repair.yml
          git add Infrastructure/Messaging/LegendConnectHistoricalReevaluationWork.cs \
                  Infrastructure/Messaging/LegendConnectCurriculumManifestProcessing.cs \
                  AgentPortal/Services/LegendFounderAiConversationService.cs \
                  AgentPortal.Tests/LegendConnectHistoricalReevaluationWorkTests.cs \
                  AgentPortal.Tests/LegendFounderAiConversationRoutingTests.cs
          git diff --cached --check
          git commit -m 'Retire terminal LEGEND failures and surface exact reasons'
          git push origin HEAD:fix/legend-terminal-retirement-reasons-20260824
