.qv-booking-modal-shell{
    --qv-royal-950:#09122f;
    --qv-royal-925:#0d1a44;
    --qv-royal-900:#112356;
    --qv-royal-850:#173171;
    --qv-royal-800:#214396;
    --qv-royal-760:#3159bf;
    --qv-royal-line:rgba(125,150,255,.20);
    --qv-royal-line-strong:rgba(143,170,255,.34);
    --qv-royal-glow:rgba(77,111,255,.24);
    --qv-gold:#f0ce70;
}

.qv-booking-launch{
    white-space:nowrap;
}

.qv-booking-modal-shell .modal-dialog{
    width:min(93vw, 1140px);
    max-width:1140px;
}

.qv-booking-modal-shell .modal-content{
    border:1px solid rgba(240,206,112,.28);
    border-radius:30px;
    background:
        radial-gradient(720px 320px at 50% -6%, rgba(49,89,191,.26), transparent 58%),
        linear-gradient(180deg, rgba(11,25,66,.985), rgba(8,18,46,.995));
    box-shadow:
        0 34px 80px rgba(2,8,23,.54),
        inset 0 1px 0 rgba(255,255,255,.05);
    color:#f8fbff;
    overflow:hidden;
    height:auto;
    max-height:min(calc(100vh - 24px), 820px);
    display:grid;
    grid-template-rows:auto auto;
}

.qv-booking-head{
    display:flex;
    align-items:flex-start;
    justify-content:space-between;
    gap:16px;
    padding:18px 22px 14px;
    border-bottom:1px solid rgba(255,255,255,.08);
}

.qv-booking-head-copy{
    display:grid;
    gap:6px;
    min-width:0;
}

.qv-booking-kicker{
    color:#f0ce70;
    font-size:.72rem;
    font-weight:950;
    letter-spacing:.12em;
    text-transform:uppercase;
}

.qv-booking-title{
    margin:0;
    color:#f8fbff;
    font-size:1.62rem;
    font-weight:960;
    line-height:1.02;
    letter-spacing:-.025em;
}

.qv-booking-sub{
    margin:0;
    color:rgba(226,232,240,.76);
    font-size:.88rem;
    line-height:1.42;
}

.qv-booking-close{
    flex:0 0 auto;
    min-width:96px;
}

.qv-booking-body{
    display:grid;
    grid-template-rows:auto auto;
    gap:12px;
    padding:14px 22px 18px;
    overflow:auto;
    min-height:0;
}

.qv-booking-record{
    display:grid;
    gap:4px;
    padding:10px 14px;
    border-radius:18px;
    border:1px solid var(--qv-royal-line-strong);
    background:linear-gradient(180deg, rgba(24,46,109,.92), rgba(14,28,70,.98));
}

.qv-booking-record-label{
    color:rgba(240,206,112,.8);
    font-size:.72rem;
    font-weight:900;
    letter-spacing:.08em;
    text-transform:uppercase;
}

.qv-booking-record-name{
    color:#ffffff;
    font-size:1rem;
    font-weight:920;
    line-height:1.2;
}

.qv-booking-shell{
    display:grid;
    gap:0;
    padding:0;
    border-radius:24px;
    border:1px solid var(--qv-royal-line);
    background:
        radial-gradient(640px 280px at 12% 0%, rgba(77,111,255,.18), transparent 60%),
        linear-gradient(180deg, rgba(13,30,78,.97), rgba(8,19,49,.99));
    overflow:hidden;
    min-height:0;
    height:auto;
}

.qv-booking-date-native{
    position:absolute;
    opacity:0;
    pointer-events:none;
    width:1px;
    height:1px;
}

.qv-booking-layout{
    display:flex;
    align-items:flex-start;
    gap:0;
    min-height:0;
    height:auto;
}

.qv-booking-sidebar{
    display:flex;
    flex-direction:column;
    gap:10px;
    padding:12px;
    border-right:1px solid var(--qv-royal-line);
    background:
        linear-gradient(180deg, rgba(10,24,63,.96), rgba(8,18,46,.99));
    min-height:0;
    align-self:flex-start;
    flex:0 0 336px;
    width:336px;
}

.qv-booking-main{
    display:flex;
    flex-direction:column;
    gap:10px;
    padding:12px;
    min-width:0;
    min-height:0;
    align-self:flex-start;
    flex:1 1 auto;
}

.qv-booking-panel{
    display:grid;
    gap:10px;
    padding:12px;
    border-radius:20px;
    border:1px solid var(--qv-royal-line);
    background:linear-gradient(180deg, rgba(11,24,58,.58), rgba(7,16,37,.54));
}

.qv-booking-panel-calendar{
    gap:12px;
}

.qv-booking-panel-head{
    display:grid;
    gap:8px;
    align-items:start;
}

.qv-booking-panel-copy{
    display:grid;
    gap:4px;
}

.qv-booking-panel-kicker{
    color:rgba(240,206,112,.9);
    font-size:.72rem;
    font-weight:950;
    letter-spacing:.1em;
    text-transform:uppercase;
}

.qv-booking-panel-title{
    color:#f8fbff;
    font-size:1rem;
    font-weight:920;
    line-height:1.2;
}

.qv-booking-panel-note{
    color:rgba(226,232,240,.7);
    font-size:.8rem;
    line-height:1.38;
}

.qv-booking-field{
    display:grid;
    gap:6px;
    min-width:0;
}

.qv-booking-field span{
    color:rgba(240,206,112,.86);
    font-size:.7rem;
    font-weight:900;
    letter-spacing:.08em;
    text-transform:uppercase;
}

.qv-booking-field input,
.qv-booking-field select{
    min-height:46px;
    border:1px solid rgba(130,155,255,.34);
    border-radius:14px;
    padding:10px 12px;
    background:linear-gradient(180deg, rgba(245,248,255,.98), rgba(234,241,252,.97));
    color:#0f172a;
    box-shadow:inset 0 1px 0 rgba(255,255,255,.56);
}

.qv-booking-field input:focus,
.qv-booking-field select:focus{
    outline:none;
    border-color:rgba(240,206,112,.62);
    box-shadow:0 0 0 4px rgba(49,89,191,.18);
}

.qv-booking-field-compact{
    width:100%;
    max-width:none;
    justify-self:stretch;
    align-self:start;
}

.qv-booking-calendar-card{
    display:grid;
    gap:10px;
    padding:12px;
    border-radius:18px;
    border:1px solid rgba(240,206,112,.24);
    background:
        radial-gradient(260px 140px at 50% 0%, rgba(240,206,112,.12), transparent 72%),
        linear-gradient(180deg, rgba(24,32,68,.84), rgba(10,18,42,.80));
    box-shadow:
        inset 0 1px 0 rgba(255,255,255,.03),
        0 0 0 1px rgba(240,206,112,.05);
}

.qv-booking-calendar-head{
    display:grid;
    grid-template-columns:auto 1fr auto;
    align-items:center;
    gap:10px;
}

.qv-booking-calendar-nav{
    min-width:64px;
    min-height:38px;
    padding:0 10px;
    border-color:rgba(240,206,112,.42);
    background:linear-gradient(180deg, rgba(240,206,112,.16), rgba(240,206,112,.08));
    color:#f4d98a;
}

.qv-booking-calendar-label{
    text-align:center;
    color:#f4d98a;
    font-size:.95rem;
    font-weight:900;
    letter-spacing:-.01em;
}

.qv-booking-calendar-weekdays{
    display:grid;
    grid-template-columns:repeat(7, minmax(0, 1fr));
    gap:6px;
}

.qv-booking-calendar-weekdays span{
    text-align:center;
    color:rgba(244,217,138,.72);
    font-size:.7rem;
    font-weight:900;
    letter-spacing:.08em;
    text-transform:uppercase;
}

.qv-booking-calendar-grid{
    display:grid;
    grid-template-columns:repeat(7, minmax(0, 1fr));
    grid-auto-rows:auto;
    gap:6px;
    min-height:0;
}

.qv-cal-day{
    position:relative;
    display:grid;
    place-items:center;
    aspect-ratio:1 / 1;
    min-height:0;
    padding:0;
    border-radius:14px;
    border:1px solid rgba(240,206,112,.24);
    background:linear-gradient(180deg, rgba(32,40,79,.72), rgba(15,22,48,.68));
    color:#f8fbff;
    font-size:.88rem;
    font-weight:850;
    transition:border-color .18s ease, transform .18s ease, background .18s ease, color .18s ease, box-shadow .18s ease;
}

.qv-cal-day:hover:not(.is-disabled){
    transform:translateY(-1px);
    border-color:rgba(240,206,112,.48);
    background:linear-gradient(180deg, rgba(78,61,20,.52), rgba(39,32,12,.56));
}

.qv-cal-day.is-outside{
    color:rgba(244,217,138,.34);
    background:rgba(255,255,255,.01);
}

.qv-cal-day.is-disabled{
    opacity:.38;
    cursor:not-allowed;
    filter:saturate(.8);
}

.qv-cal-day.is-today{
    border-color:rgba(240,206,112,.64);
    box-shadow:
        inset 0 0 0 1px rgba(240,206,112,.18),
        0 0 0 1px rgba(240,206,112,.08);
}

.qv-cal-day.is-selected{
    color:#0f172a;
    border-color:rgba(255,239,180,.84);
    background:linear-gradient(180deg, #f8e2a1 0%, #e3ba57 100%);
    box-shadow:
        0 0 0 1px rgba(255,239,180,.22),
        0 10px 18px rgba(214,168,61,.22);
}

.qv-booking-selection-date{
    color:#ffffff;
    font-size:1rem;
    font-weight:920;
    line-height:1.24;
}

.qv-booking-selection-meta{
    color:rgba(226,232,240,.74);
    font-size:.82rem;
    line-height:1.48;
}

.qv-booking-slot-card{
    display:flex;
    flex-direction:column;
    gap:10px;
    min-height:0;
    padding:14px;
    border-radius:22px;
    border:1px solid var(--qv-royal-line);
    background:
        radial-gradient(520px 220px at 100% 0%, rgba(77,111,255,.15), transparent 56%),
        linear-gradient(180deg, rgba(10,22,56,.72), rgba(7,16,38,.7));
    min-width:0;
    flex:0 0 auto;
}

.qv-booking-slot-head{
    display:flex;
    align-items:flex-start;
    justify-content:space-between;
    gap:10px;
    flex-wrap:wrap;
}

.qv-booking-slot-title{
    color:#f8fbff;
    font-size:1rem;
    font-weight:930;
}

.qv-booking-slot-note{
    color:rgba(226,232,240,.72);
    font-size:.78rem;
    line-height:1.32;
    margin-top:2px;
}

.qv-booking-slot-focus{
    display:inline-flex;
    align-items:center;
    min-height:36px;
    padding:7px 12px;
    border-radius:999px;
    border:1px solid rgba(143,170,255,.28);
    background:rgba(49,89,191,.22);
    color:#fff2c2;
    font-size:.8rem;
    font-weight:860;
    text-align:center;
}

.qv-book-slots{
    display:grid;
    align-content:start;
    grid-template-columns:repeat(4, minmax(145px, 1fr));
    grid-auto-rows:minmax(44px, auto);
    gap:10px;
    width:100%;
    min-width:0;
}

.qv-slot-btn{
    position:relative;
    min-height:44px;
    border-radius:14px;
    padding:10px 12px;
    border:1px solid rgba(240,206,112,.34);
    background:linear-gradient(180deg, rgba(43,47,56,.96), rgba(26,31,42,.98));
    color:#f2d688;
    font-size:.92rem;
    font-weight:880;
    line-height:1.15;
    box-shadow:
        inset 0 1px 0 rgba(255,255,255,.05),
        0 8px 16px rgba(5,10,24,.16);
    transition:
        transform .16s ease,
        border-color .16s ease,
        background .16s ease,
        color .16s ease,
        box-shadow .16s ease;
}

.qv-slot-btn:hover{
    border-color:rgba(240,206,112,.52);
    background:linear-gradient(180deg, rgba(52,57,68,.98), rgba(31,36,48,1));
    transform:translateY(-1px);
    box-shadow:
        inset 0 1px 0 rgba(255,255,255,.06),
        0 12px 20px rgba(5,10,24,.22);
}

.qv-slot-btn:focus-visible{
    outline:none;
    border-color:rgba(255,255,255,.84);
    box-shadow:
        0 0 0 2px rgba(255,255,255,.20),
        0 0 0 5px rgba(220,176,84,.28);
}

.qv-slot-btn.selected,
.qv-slot-btn.selected:hover,
.qv-slot-btn.selected:focus-visible{
    border-color:rgba(255,255,255,.94);
    background:
        linear-gradient(180deg, rgba(55,82,155,.98), rgba(20,34,77,.99));
    color:#ffffff;
    text-shadow:0 1px 0 rgba(0,0,0,.22);
    box-shadow:
        inset 0 1px 0 rgba(255,255,255,.22),
        0 0 0 2px rgba(255,255,255,.18),
        0 0 0 5px rgba(220,176,84,.34),
        0 14px 26px rgba(6,14,36,.34);
    transform:translateY(-1px) scale(1.01);
}

.qv-slot-empty{
    display:flex;
    align-items:center;
    justify-content:center;
    min-height:180px;
    padding:18px;
    border:1px dashed rgba(132,160,255,.28);
    border-radius:18px;
    background:linear-gradient(180deg, rgba(18,35,84,.42), rgba(9,19,47,.4));
    color:rgba(226,232,240,.74);
    font-size:1rem;
    font-weight:760;
    text-align:center;
}

.qv-booking-foot{
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:10px;
    flex-wrap:wrap;
}

.qv-booking-status{
    display:inline-flex;
    align-items:center;
    min-height:44px;
    padding:10px 13px;
    border-radius:14px;
    border:1px solid var(--qv-royal-line);
    background:linear-gradient(180deg, rgba(21,38,90,.44), rgba(12,22,52,.4));
    color:rgba(226,232,240,.78);
    font-size:.86rem;
    font-weight:820;
    flex:1 1 280px;
}

.qv-booking-status[data-state="loading"]{
    color:#f8fbff;
    border-color:rgba(143,170,255,.32);
    background:rgba(49,89,191,.18);
}

.qv-booking-status[data-state="selected"]{
    color:#fff2c2;
    border-color:rgba(240,206,112,.34);
    background:rgba(240,206,112,.12);
}

.qv-booking-status[data-state="warning"]{
    color:#fde68a;
    border-color:rgba(245,158,11,.34);
    background:rgba(245,158,11,.12);
}

.qv-booking-status[data-state="success"]{
    color:#dcfce7;
    border-color:rgba(34,197,94,.34);
    background:rgba(34,197,94,.14);
}

.qv-booking-status[data-state="error"]{
    color:#fecaca;
    border-color:rgba(248,113,113,.34);
    background:rgba(248,113,113,.12);
}

.qv-booking-submit{
    min-width:188px;
    min-height:46px;
}

@media (min-width: 1100px){
    .qv-booking-panel-selection{
        display:none;
    }
}

@media (max-width: 1365.98px){
    .qv-booking-modal-shell .modal-dialog{
        width:min(95vw, 1160px);
        max-width:1160px;
    }

    .qv-booking-sidebar,
    .qv-booking-main{
        padding:12px;
    }

    .qv-booking-sidebar{
        flex-basis:320px;
        width:320px;
    }

    .qv-booking-slot-card{
        padding:14px;
    }
}

@media (max-width: 1180.98px){
    .qv-booking-modal-shell .modal-content{
        max-height:min(calc(100vh - 18px), 800px);
    }

    .qv-booking-head{
        padding:16px 18px 12px;
    }

    .qv-booking-body{
        padding:12px 18px 16px;
    }

    .qv-booking-title{
        font-size:1.48rem;
    }

    .qv-booking-layout{
        gap:0;
    }

    .qv-booking-sidebar{
        flex-basis:304px;
        width:304px;
    }

    .qv-booking-panel,
    .qv-booking-slot-card{
        padding:13px;
    }

    .qv-booking-calendar-grid{
        grid-auto-rows:minmax(36px, auto);
        min-height:252px;
    }

    .qv-cal-day{
        min-height:36px;
        font-size:.84rem;
    }

    .qv-slot-btn{
        min-height:42px;
        font-size:.88rem;
    }
}

@media (max-width: 1099.98px){
    .qv-booking-modal-shell .modal-dialog{
        width:min(96vw, 1040px);
    }

    .qv-booking-modal-shell .modal-content{
        max-height:min(calc(100vh - 16px), 900px);
    }

    .qv-booking-layout{
        flex-direction:column;
        min-height:0;
    }

    .qv-booking-sidebar{
        width:100%;
        flex-basis:auto;
        border-right:0;
        border-bottom:1px solid rgba(255,255,255,.08);
    }

    .qv-booking-slot-card{
        min-height:0;
    }

    .qv-book-slots{
        grid-template-columns:repeat(3, minmax(0, 1fr));
    }
}

@media (max-width: 929.98px){
    .qv-booking-modal-shell .modal-dialog{
        width:min(97vw, 880px);
        max-width:880px;
    }

    .qv-booking-modal-shell .modal-content{
        max-height:min(calc(100vh - 14px), 860px);
    }

    .qv-booking-head{
        padding:16px 18px 12px;
    }

    .qv-booking-body{
        padding:14px 18px 16px;
        gap:12px;
    }

    .qv-booking-title{
        font-size:1.48rem;
    }

    .qv-booking-sidebar,
    .qv-booking-main{
        padding:12px;
    }

    .qv-booking-panel-head{
        gap:8px;
    }

    .qv-book-slots{
        grid-template-columns:repeat(2, minmax(0, 1fr));
    }
}

@media (max-width: 767.98px){
    .qv-booking-modal-shell .modal-dialog{
        width:calc(100vw - 12px);
        margin:6px auto;
    }

    .qv-booking-modal-shell .modal-content{
        border-radius:24px;
        height:calc(100vh - 12px);
        max-height:calc(100vh - 12px);
    }

    .qv-booking-head{
        padding:18px 16px 14px;
        gap:12px;
    }

    .qv-booking-body{
        padding:16px;
        gap:14px;
    }

    .qv-booking-title{
        font-size:1.4rem;
    }

    .qv-booking-record{
        padding:14px 15px;
        border-radius:18px;
    }

    .qv-booking-sidebar,
    .qv-booking-main{
        padding:14px;
    }

    .qv-booking-panel,
    .qv-booking-slot-card{
        padding:14px;
        border-radius:18px;
    }

    .qv-booking-panel-head,
    .qv-booking-slot-head{
        gap:12px;
    }

    .qv-booking-calendar-head{
        gap:8px;
    }

    .qv-booking-calendar-nav{
        min-width:62px;
    }

    .qv-booking-calendar-weekdays,
    .qv-booking-calendar-grid{
        gap:6px;
    }

    .qv-cal-day{
        min-height:38px;
        border-radius:14px;
        font-size:.88rem;
    }

    .qv-book-slots{
        grid-template-columns:1fr;
        gap:10px;
    }

    .qv-slot-btn{
        min-height:50px;
        font-size:.9rem;
    }

    .qv-slot-empty{
        min-height:160px;
        font-size:.92rem;
    }

    .qv-booking-panel-selection{
        display:grid;
    }

    .qv-booking-foot{
        flex-direction:column;
        align-items:stretch;
    }

    .qv-booking-status,
    .qv-booking-submit{
        width:100%;
        min-width:0;
    }
}
