# Computed graph composition repair

An independent reproduction extends the existing typed executor control (rate multiplied by quantity, then fee added) through ordinary curriculum admission, source grounding, planning and native reply. Its teaching examples exclude the original control values and result. Frozen capability prompts, expected answers, thresholds and foundation curriculum are unchanged.

At `dc0e683e`, the first computed relationship failed with `governed_computation_schema_invalid`. Source review identified a fixture error: its observed subtotal example retained a fee without declaring the existing source-bound fee passthrough. Commit `4c9eeaac` corrects that declaration without changing assertions. At `ae4bc697`, the corrected fixture still failed at `PersistFounderCrossExampleSemanticRelationAsync`; 27 accompanying research policy/diagnostic cases passed.

The remaining contract mismatch is structural: `TryGraphSemanticValues` supplies the exact directed subtotal-to-fee graph coordinate as `present`, while arithmetic result validation rejects all literal conclusions. Dropping the graph loses the next operation's premise. Allowing arbitrary `rel_` names would grant unsupported facts.

The repair introduces an internal graph receipt within the existing computation authority. Admission derives coordinates from actual governed result relations and endpoints. Serving reconstructs current source/result graph frames, verifies the current template and operator parent, and selects a receipt from a supported, counted example pair in the same transition group. The executor permits only its exact graph coordinates and still rejects unrelated literal facts, altered frames and conflicting conclusions. This is a controlled composition extension, not proof of general language understanding.

Current graph revalidation adds three bounded batched queries only for computed results containing literal coordinates. Empty scopes stop early; ordinary computed results retain their existing path. Limits and overflow refusal are explicit. Their Azure SQL latency remains unverified.

The SQL diagnostic amendment recognizes only the three exact static query tags and adds attribution regression cases. It changes no SELECT-only permission rule, result requirement, capability assertion, threshold or release gate. New SQLite execution coverage checks actual relational translation, bounded query execution and withdrawal; this is not Azure execution proof.

Combined verification is recorded in final-verification.json. Earlier red reproductions are preserved under `/private/tmp/legend-composition-preservation-ae4bc697` and must not be relabeled as passes.

At `21213eaf`, all 154 focused cases passed, including the complete two-step native reply, copied-pair rejection, evidence withdrawal and actual bounded SQLite execution. At final source `5798e251`, the full suite reports 2,350 runner passes, nine unchanged capability failures and four skipped facts. Two resource facts passed separately; two SQL facts remain unexecuted for this candidate. Twenty-four existing gated runner passes have no live-data credit. This repair does not establish general prose understanding or release readiness.
