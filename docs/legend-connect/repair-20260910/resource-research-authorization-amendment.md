# Resource research authorization prerequisite amendment

The live resource diagnostic on candidate `4d467338778237687a86b54fafb17b25bdeed7aa`, run `local:resource:48bedcf3ec0c4f1ab4c3be4a855e33b9`, failed before HTTP execution. Its receipt reported the named-source research decision, then `research_public_authorization_invalid` at `governed_research`, zero HTTP calls, zero canonical write attempts, and three local observability writes. The final assertion stage was `local_observability`; the earlier structured outcome identifies the actual rejected prerequisite.

The fixture supplied the invented authorization provenance `ExplicitResourceDiagnosticOptIn`. The existing production validator accepts public requests only with `PublicAuthorizationProvenance` or `LockedEvaluationAuthorizationProvenance`. Native-only execution returns before this validation and does not consume or mutate the immutable request; reusing the request was not the cause.

This amendment changes only the test fixture to the existing `LockedEvaluationAuthorizationProvenance`. It does not represent an authenticated Founder session. The diagnostic is an explicitly selected, non-serving, noncanonical evaluator of a fixed public RFC 9110 question with local in-memory observability. The production validator and authorization allowlist are unchanged. Added assertions require evaluator provenance, no retained result, and rejection by the existing external-observation retention authority. A direct validator regression retains the old invalid label as a negative case and validates the existing evaluator label as a positive case.

The original public question, research and native-only behavioral assertions, transport limits, canonical write guard, and held-out prompts, answers, exclusions, and thresholds remain unchanged. A pass still requires actual HTTP evidence and a non-Failure governed outcome. It remains a resource-boundary observation, not a native-answer capability pass or production learning proof. The original failed receipt remains evidence of the failed candidate and must not be relabeled as successful.

No tests, builds, provider calls, production writes, or credential reads were performed while authoring this amendment. Root verification must run the direct regression and the selected live test on the new exact candidate with matching assembly identity before claiming repaired resource execution.
