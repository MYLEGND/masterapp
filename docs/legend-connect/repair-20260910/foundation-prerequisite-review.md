# Controlled foundation prerequisite amendment

Scope: test setup only. Frozen requests, expected answers, thresholds, exclusions and existing capability assertions remain unchanged. The nine capability gates still require successful native answers; this amendment does not claim they pass. No production Founder approval, production curriculum mutation or deployed behavior is established by test fixtures.

Source checkpoint: `0ec4ea35deb33e3f918f346c52b48f4721c40cfc`.

| Existing source | Git blob | Reused declarations |
| --- | --- | --- |
| `AgentPortal.Tests/LegendConnectComputedLanguageEndToEndContractTests.cs` | `3b22302cab33527df80006a7ed30903ce0456d3b` | Existing integer teaching: amber/copper/silver, two operand examples each, subtraction relationships and response layouts |
| `AgentPortal.Tests/LegendConnectNamedValueLanguageEndToEndTests.cs` | `decf233aa24642c802ef15a9c8dca66bbfb4897b` | Existing Anika/Benoit/Carla ownership and opaque date teaching plus recall declarations |
| `AgentPortal.Tests/LegendConnectBatchLanguageEndToEndContractTests.cs` | `7af443b290d7ae91fa69ba691757f66240771ebf` | Existing six homogeneous batch examples and their computed operator relationships |

The named-value and batch admission loops are extracted into internal helpers. Existing tests call them with their original defaults. Numeric and batch helpers accept optional semantic-example identity namespaces because both fixtures previously used the same twelve globally canonical source/result keys in separate databases. Only declaration keys and matching relationship endpoint references gain prefixes. Teaching text, values, families, semantic frames, graph node identifiers and operator labels remain unchanged.

The shared prerequisite admits all three fixed fixtures through the existing curriculum submission and cross-example relation authorities before the held-out write sentinel is armed. Teaching helpers receive no held-out prompt or expected answer. The same corpus is applied without selecting declarations according to capability category or observed failures.

After admission, separation checks examine every text unit, including derived units: no normalized complete frozen request may occur, nor the distinctive names Devon, Kestrel, Marlin, Corine or the target fraction 2/17. Shared ordinary words and small numbers are not contamination by themselves; rejecting them would disallow independent arithmetic examples. Source declarations are preserved rather than curated to maximize held-out results. This is not a claim of independent statistical evaluation or general-language coverage.

The memory setup and the native capability/isolation setup receive the prerequisite. Provider-only tests and separately admitted semantic evidence cases retain their existing setup. Full regression must establish that combining previously separate corpora has not introduced conflicting meaning or invalidated the admitted operator contracts. Source review alone does not establish successful admission or execution.

The existing write sentinel and all answer/authority/provider/write assertions remain authoritative after setup. Heterogeneous priority/unique-contact planning, broader prose interpretation and remaining unknown wording must continue to fail when unsupported.
