# Research evidence observation coverage

Candidate `a90a03ffdcfbbccf8f3a57a735cc77d9058d6da8` executed the authorized RFC 9110 resource probe and returned `InsufficientEvidence`. Its preserved receipt reports one query, eight page receipts, one document, one citation, approximately 54.85 seconds searching, 3.35 seconds retrieving, and 15 milliseconds reasoning. This establishes provider and governed assessment execution, not a correct answer.

The resource diagnostic serialized only Failure.ReasonCode and Session.FailureReason. It omitted the InsufficientEvidence and UnresolvedConflict reason codes, evidence admissibility receipts, claim resolutions, and citation validation. Therefore its null reason cannot distinguish failed passage entailment, source provenance, timestamps, authority scope, independent evidence, or presentation validation. HTTP 200 and one citation alone do not establish admissible supporting evidence.

This amendment reports those existing outcomes using bounded counts, enum states, and the existing telemetry reason normalizer. It adds only exact reason literals already emitted by the current research assessment and page authorities to that normalizer's finite allowlist. It exports no questions, statements, passages, source identities, URLs, provider text, or exception messages. Tests preserve actual reason codes and reject invented identifiers and private-text suffixes.

No source threshold is changed. The existing request factory already requires one independent source for NamedExternalDocumentOrSource. The existing `DefinitivePrimaryRecord_ControlsItsExactClaimWithoutSyntheticCorroboration` regression demonstrates that a controlling exact claim can conclude with one primary record even when the requested count is three. A named-document title is not automatically such evidence: actual source scope and bound passage still must establish the claim. A future independent title-specific control should supply a single official technical document, its exact title passage and supported claim, then pair it with a mismatched-passage negative. It must exercise the existing assessment authority without treating the live RFC answer as new curriculum.

The amended report can expose the next observed rejection but cannot reconstruct the omitted details of the original run. No build, test, provider request, corpus mutation, or threshold change was performed while authoring this amendment.
