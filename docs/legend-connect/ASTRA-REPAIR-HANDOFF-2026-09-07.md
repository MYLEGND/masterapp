# LEGEND integration checkpoint — 7 September 2026

**Release decision: HOLD.** The candidate adds bounded native arithmetic, homogeneous batch scheduling, declared source-slot language grounding, and stable retained evidence. It does not establish unrestricted reasoning, general calendar planning, or performance superior to another model. Production release gates remain authoritative.

## Candidate identity and ownership

- Production base reconciled into this candidate: `9f07f3cca17cd167dcef89db220c0e1fe1f0c618` (PR101). Earlier PR100 (`9ab6ba49`) is also preserved. These are branch identities; the deployed application's reported SHA has not been independently verified.
- Local integration branch: `repair/astra-legend-final-20260907`.
- Final code candidate: `e3609a557222317b2cc226cd49db21c5f1246145`; code tree: `97ec9d04468b2ee1929ac8910a3373ffef1f5448`. This report is a documentation-only follow-up; exact remote SHA verification is separate.
- The same six specialist worktrees handled runtime, native reasoning, retained knowledge, governed tools, independent verification, and independent release review. No replacement agents or duplicate inference/validation authorities were introduced. Only the lead ran .NET builds and test suites, serially; governed-tools also ran the isolated Python runner simulations.

## Confirmed implementation

1. The existing governed executor computes bounded exact integer/decimal/rational arithmetic, comparison, and homogeneous batch schedules. Undefined calculations do not suppress independent valid proofs. Schedule allocations, elapsed time, resource bounds, and a signature are carried with the selected reasoning path through the native response. An answer model cannot replace certificate-bearing articulation without corresponding authorization. Heterogeneous hazard priorities and unique named-site contact constraints are not supplied by a homogeneous certificate.
2. Existing Founder declarations can ground entirely unseen numeric values through literal context, declared variable roles, connected semantic graphs, and independent teaching families. Fresh values are current-turn assertions with typed source receipts, never new Founder facts. Wrong numeric teaching witnesses, missing context, extra constraints, repeated separators, and invalid numbers fail closed. Retrieval and matching remain bounded and cancellable; dense-corpus capacity and live latency are unverified.
3. Retained machine learning binds to the original immutable evidence manifest in the existing proposal payload, without a schema change. Healthy corpus growth can preserve eligibility; withdrawal, replacement, changed lineage, and new contradictions cannot be hidden by substitute evidence. Legacy payload behavior is preserved and malformed server envelopes are rejected.
4. Discourse persists and revalidates original source-slot receipts. Invalidated replacement targets retain their replacement barriers so older values cannot reappear. Adding an identical exact Founder source can preserve the original receipt and current-turn provenance; changed roles or withdrawn original templates cannot. Names and opaque date strings use the same source evidence and response layout; the exact first reply and conversation-ID-only recall now pass without either value being present in the teaching corpus.
5. Indexed meaning retrieval, request timing, current-turn analysis reuse, SQL-translatable learning-panel projection, immutable native-only provider policy, exact owned-read receipts, and learning authority repairs from the earlier checkpoint remain integrated. Local translation probes do not prove production recovery or SQL concurrency.
6. The existing isolated diagnostic workflow and runner now require the exact candidate, fresh discovery/TRX/JSON evidence, a technically SELECT-only SQL principal, physical-table restrictions, bounded queries, zero production write attempts, and measured zero external clients/calls. The canonical eleven-category matrix remains intact. Thirty-one simulated runner scenarios pass; simulations are not SQL or deployment proof.

## Verification ledger

| Candidate | Execution | Result and limit |
|---|---|---|
| `982b65db` (earlier checkpoint) | Full suite | 1,770 total; 1,759 runner passes; 11 failures. Two failures were corrected timestamp assertions; nine capability failures remained. |
| `92136596` (earlier checkpoint) | Clean build and affected novelty class | 0 warnings/errors; 12 tests passed. No full rerun at that SHA. |
| `0b92d6a42` | Stage-two full solution build and full suite | 0 warnings/errors; 1,913 tests, 1,902 runner passes, nine failures, two skips. Twenty-three passes were environmental early returns, leaving 25 SQL/live opt-ins unexercised. |
| `13f96f03b` | Build and focused native language/certificate/discourse suite | 0 warnings/errors; 42/43 passed. Repeated-separator context regression identified and subsequently repaired. |
| `b524780d2` | Build and focused suite after PR101 integration | 0 warnings/errors; 47/48 passed. All three healthy-growth cases and context regressions passed. Exact name/date articulation failed due to casing/punctuation loss; recall assertions had not yet executed. |

The earlier checkpoint's count of 17 environmental early returns was incomplete. The complete stage-two audit identified 23 runner-passed early returns plus two explicit skips; none receive SQL/live-proof credit.

| Candidate | Execution | Result and limit |
|---|---|---|
| `e3609a557` | Final code build and focused suite | 0 warnings/errors, 1m36.10s; 48/48 passed, including exact name/date initial reply and later recall. |
| `e3609a557` | Complete final local suite | 1,955 total; 1,944 runner passes, nine unchanged capability failures, two SQL/live skips; 5m56s; exit 1. |

## Outstanding release gates

The original nine broader capability tests remain unchanged. Their controlled databases contain a language registry and identity fixtures but no admitted semantic curriculum; failures stop at `meaning_graph_component_unknown` with zero evidence. This is an unresolved acceptance prerequisite, not proof about the production corpus. No held-out answers were added, no assertions were weakened, and no gate was excluded.

They cover broader arithmetic/language composition, constrained planning, rewriting, deduction, causal diagnosis, Haitian Creole conflict, authorized internal-data uncertainty, conversation memory, and native tool planning. The new disjoint teaching tests establish bounded capabilities; they do not turn these nine gates green.

Live SQL, real provider acceptance, authenticated production latency, migration/deployment execution, and device behavior remain separate required evidence. Native-only is a provider policy, not a SQL read-only guarantee: ordinary full chat can persist discourse. The isolated matrix uses the production SELECT-only database and a separate in-memory discourse store.

The existing validation branch can run candidate observation, but observation remains `ReleaseProof=false`. The production workflow still requires an eligible nondraft production PR, unchanged candidate/base checks, zero-warning build, nonempty successful full regression, migration/architecture gates, deployment, and postdeployment native proof. An isolated observation cannot override a failed regression.

## Execution and rollback

Builds used SDK 10.0.400, serial MSBuild, and the existing solution/test project. Temporary build-output isolation is not a runtime authority change. Missing SQL/provider configuration was never replaced with fake live evidence. No schema or migration files changed. Reverting the integration returns to the reconciled production code; unrelated PR100/PR101 work must remain preserved.

Final candidate, exact commands/counts, remote validation identity, and any publication/configuration failure are recorded in the final execution update below.


## Final local execution and publication preparation

CONFIRMED: no new failures remain outside the original nine capability gates. Build exit was 0; focused test exit was 0; full-suite exit was 1. Production branch was rechecked at `9f07f3cc` before preparing Git objects. The remote Git tree for the code candidate was reconstructed from exact tracked bytes and modes and matched local tree `97ec9d04468b2ee1929ac8910a3373ffef1f5448`. Creating those immutable objects did not move any branch or deploy code.

The connected GitHub route is being used because ordinary Git CLI push lacks credentials. The existing validation branch is reused with a normal fast-forward only. A newly created remote commit has its own SHA; local results at `e3609a557` must not be represented as execution of that remote SHA. The existing workflow is responsible for remote candidate evidence. No production branch update, migration, replay, or deployment has been performed for this candidate.

Commands, from the repository:

```bash
/opt/dotnet/dotnet restore MASTERAPP.sln --disable-parallel -m:1 -p:DirectoryBuildPropsPath=/tmp/legend-astra/Directory.Build.props
/opt/dotnet/dotnet build MASTERAPP.sln -c Release --no-restore -m:1 -p:UseSharedCompilation=false -p:DirectoryBuildPropsPath=/tmp/legend-astra/Directory.Build.props
GITHUB_WORKSPACE=/workspace/scratch/f917e68a3fb0/masterapp LEGEND_HELDOUT_MATRIX_PATH=/tmp/legend-astra/stage2-final-local-matrix.jsonl /opt/dotnet/dotnet test AgentPortal.Tests/AgentPortal.Tests.csproj -c Release --no-build --no-restore -m:1 -p:DirectoryBuildPropsPath=/tmp/legend-astra/Directory.Build.props --logger 'trx;LogFileName=stage2-final-local.trx' --results-directory /tmp/legend-astra/results/stage2-final-local --blame-hang-timeout 90s --blame-hang-dump-type none
```

The focused run used the same test command with the seven affected class filters: computed language, batch language, schedule metadata admission, source-slot discourse retention, computed operator admission, schedule certificate propagation, and named-value language. Its TRX is `/tmp/legend-astra/results/stage2-thirteenth/stage2-thirteenth.trx`. Build log: `/tmp/legend-astra-stage2-thirteenth-build.log`. Full test log: `/tmp/legend-astra-stage2-final-local-tests.log`.

Independent final audit: all 48 focused cases also passed inside the full run. Of 1,944 runner passes, 23 SQL tests returned early (16 logged and seven silent); with the two skips, 25 SQL/live opt-ins remained unexecuted. There are 1,921 remaining local pass records. The three affected finance classes passed 30/30 cases, including nine new test identities since the earlier full run; five replace earlier finance cases, for a net increase of four. The exact-candidate local isolated preflight exited 1 with `not_configured_select_only_credential`; it opened no SQL connection and reported `ReleaseProof=false`.
