<div class="modal-backdrop" id="modalBackdrop"></div>

<div class="modal" id="colsModal">
    <div class="modal-head">
        <h3 class="modal-title">Columns</h3>
        <button class="btn btn-ghost" type="button" data-closemodal>Close</button>
    </div>
    <div class="modal-body" id="colsBody"></div>
</div>

<div class="modal" id="shortcutsModal">
    <div class="modal-head">
        <h3 class="modal-title">Shortcuts</h3>
        <button class="btn btn-ghost" type="button" data-closemodal>Close</button>
    </div>
    <div class="modal-body">
        <div><span class="kbd">/</span> focus search</div>
        <div><span class="kbd">Ctrl</span> + <span class="kbd">K</span> (or <span class="kbd">Cmd</span> + <span class="kbd">K</span>) command palette</div>
        <div><span class="kbd">Enter</span> open Quick View on focused row</div>
        <div><span class="kbd">Alt</span> + <span class="kbd">N</span> mark No Answer in quick view</div>
        <div><span class="kbd">Alt</span> + <span class="kbd">B</span> mark Booked in quick view</div>
        <div><span class="kbd">Alt</span> + <span class="kbd">D</span> set next day</div>
        <div><span class="kbd">Alt</span> + <span class="kbd">E</span> create Outlook event</div>
        <div><span class="kbd">Alt</span> + <span class="kbd">M</span> move to Meeting Scheduled</div>
        <div><span class="kbd">Esc</span> close menus/drawer/modals</div>
    </div>
</div>

<div class="modal" id="remindersModal">
    <div class="modal-head">
        <h3 class="modal-title">Reminders</h3>
        <button class="btn btn-ghost" type="button" data-closemodal>Close</button>
    </div>
    <div class="modal-body">
        <div class="tiny" style="margin-bottom:10px;">
            Due Today + Overdue Next Actions from persisted CRM data. Keep the standard: no drops.
        </div>
        <div id="remindersBody"></div>
    </div>
</div>

<div class="modal" id="cmdModal">
    <div class="modal-head">
        <h3 class="modal-title">Command Palette</h3>
        <button class="btn btn-ghost" type="button" data-closemodal>Close</button>
    </div>
    <div class="modal-body">
        <div class="f">
            <label>Type a command</label>
            <input id="cmdInput" placeholder="e.g. export, copy emails, reminders, view cards, filter overdue..." />
        </div>
        <div class="tiny" style="margin-top:10px;">
            Commands: export • copy emails • reminders • enable reminders • view cards • filter overdue • filter needs • density compact • density comfort
        </div>
        <div class="tiny" style="margin-top:10px;">
            Calendar: connect calendar • save zoom link • clear zoom link • create event.
        </div>
    </div>
</div>

<div class="modal" id="importModal">
    <div class="modal-head">
        <h3 class="modal-title">Import Leads (CSV)</h3>
        <button class="btn btn-ghost" type="button" data-closemodal>Close</button>
    </div>
    <div class="modal-body">
        <div class="tiny" style="margin-bottom:10px;">
            Save your Excel sheet as CSV (UTF-8). Required column order (no extra columns): First Name, Last Name, Address, City, State, County, Zip Code, Age, DOB, M/F, Lender, Loan, Phone #, Phone # 2. Everything is created as a Lead in the Lead bucket. Max 500 rows per upload.
        </div>
        <div class="f">
            <label for="importFile">CSV file</label>
            <input type="file" id="importFile" accept=".csv,text/csv" />
        </div>
        <div class="tiny" style="margin-top:8px;">
            <a class="link" href="/Clients/ImportTemplateCsv">Download template CSV</a>
        </div>
        <div class="tiny" id="importResult" style="margin-top:8px;"></div>
        <div style="margin-top:12px; display:flex; gap:10px; justify-content:flex-end;">
            <button type="button" class="btn btn-ghost" data-closemodal>Cancel</button>
            <button type="button" class="btn btn-gold" id="btnImportSubmit">Import Leads</button>
        </div>
    </div>
</div>

<div class="modal" id="bulkModal">
    <div class="modal-head">
        <h3 class="modal-title">Bulk CRM Update</h3>
        <button class="btn btn-ghost" type="button" data-closemodal>Close</button>
    </div>
    <div class="modal-body">
        <div class="tiny" style="margin-bottom:10px;">Apply stage, next action, waiting-on, priority, tags, or a shared note to the selected contacts.</div>
        <div class="grid2">
            <div class="f">
                <label>Stage</label>
                <select id="bStage">
                    <option value="">No change</option>
                    <option value="NewLead">Lead</option>
                    <option value="Opportunities">Opportunities</option>
                    <option value="Contacted">Contacted</option>
                    <option value="Qualified">Qualified</option>
                    <option value="Client">Clients</option>
                    <option value="BusinessClient">Business Clients</option>
                    <option value="MeetingScheduled">Meeting Scheduled</option>
                    <option value="ProposalSent">Proposal Sent</option>
                    <option value="ApplicationStarted">Application Started</option>
                    <option value="Submitted">Submitted</option>
                    <option value="ClosedLost">Not Moving Forward</option>
                    <option value="Nurture">Nurture</option>
                </select>
            </div>
            <div class="f">
                <label>Waiting On</label>
                <select id="bWaitingOn">
                    <option value="">No change</option>
                    <option value="WaitingOnAgent">Waiting On Agent</option>
                    <option value="WaitingOnClient">Waiting On Client</option>
                    <option value="WaitingOnCarrier">Waiting On Carrier</option>
                    <option value="WaitingOnUnderwriting">Waiting On Underwriting</option>
                    <option value="WaitingOnDocs">Waiting On Docs</option>
                </select>
            </div>
            <div class="f">
                <label>Priority</label>
                <select id="bPriority">
                    <option value="">No change</option>
                    <option value="Urgent">Urgent</option>
                    <option value="High">High</option>
                    <option value="Normal">Normal</option>
                    <option value="Low">Low</option>
                </select>
            </div>
            <div class="f">
                <label>Next Action Date</label>
                <input type="date" id="bNextDate" />
            </div>
        </div>
        <div class="f" style="margin-top:10px;">
            <label>Next Action Text</label>
            <input id="bNextText" placeholder="Optional shared next step" />
        </div>
        <div class="f" style="margin-top:10px;">
            <label>Tags</label>
            <input id="bTags" placeholder="Comma-separated tags" />
        </div>
        <div class="f" style="margin-top:10px;">
            <label>Shared Note</label>
            <textarea id="bSharedNote" placeholder="Log one shared note to every selected client timeline."></textarea>
        </div>
        <div style="margin-top:12px; display:flex; gap:10px; justify-content:flex-end;">
            <button type="button" class="btn btn-ghost" data-closemodal>Cancel</button>
            <button type="button" class="btn btn-gold" id="btnRunBulk">Run Bulk Update</button>
        </div>
    </div>
</div>

<div class="modal" id="callTaskModal">
    <div class="modal-head">
        <h3 class="modal-title">Call Task Mode</h3>
        <button class="btn btn-ghost" type="button" data-closemodal>Close</button>
    </div>
    <div class="modal-body">
        <div class="tiny" style="margin-bottom:10px;">Fast call workflow for urgent follow-up. Use keyboard shortcuts or one-click outcomes to move fast.</div>
        <div id="callTaskBody"></div>
    </div>
</div>

<div class="toast" id="toast">Copied</div>
