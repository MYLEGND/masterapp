# Production deployment trigger

This no-op marker intentionally triggers the existing protected AgentPortal production deployment workflow for the already-validated production state. It changes no application behavior, database schema, curriculum data, replay state, or runtime configuration.
